---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/xmlutil/ns.go
  - internal/xmlutil/encoder.go
  - internal/xmlutil/decoder.go
  - internal/xmlutil/rawxml.go
  - internal/xmlutil/xmlutil_test.go
  - internal/wml/namespaces.go
  - internal/wml/document.go
  - internal/wml/properties.go
  - internal/wml/styles.go
  - internal/wml/numbering.go
  - internal/wml/table.go
  - internal/wml/wml_test.go
autonomous: true
requirements: [OPC-03, WML-01, WML-02, WML-03, WML-04]
must_haves:
  truths:
    - Any prefix bound to a known OOXML URI resolves identically on read (Word/LO/GDocs files parse to same model)
    - Marshaled output uses canonical prefixes (w, r, a, wp…) with one xmlns set on the root element
    - ~62 WML types round-trip marshal→unmarshal→marshal byte-identically
    - xml:space="preserve" is honored on read and re-emitted exactly when text needs it
    - Unknown child elements are hoarded and re-emitted verbatim (WML-04)
  artifacts:
    - internal/xmlutil (Registry, Encoder, SafeDecoder, RawXML)
    - internal/wml (~62 exported CT_* types across 5 files + namespaces.go)
  key_links:
    - xmlutil.Registry maps both Transitional and Strict URI families to canonical prefixes
    - RawXML implements xml.Marshaler/Unmarshaler and is the `xml:",any"` catch-all field type on containers
    - xmlutil.Encoder rewrites xml.Name{Space:URI}→prefix:local before delegating to xml.Encoder
---

<objective>
Deliver `internal/xmlutil` (URI↔prefix registry, canonical-prefix write encoder, safe decoder, RawXML token-blob capture) and `internal/wml` (~62 essential WordprocessingML struct types) with full round-trip test coverage.

Purpose: Namespace correctness (OPC-03) and WML fidelity (WML-01..04) are the schema foundation every later phase uses.
Output: internal/xmlutil (4 files + tests), internal/wml (6 files + tests).
</objective>

<execution_context>
@/Users/fabiomarini/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/fabiomarini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PRD.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: xmlutil — namespace registry, safe decoder, RawXML, canonical-prefix encoder</name>
  <files>internal/xmlutil/ns.go, internal/xmlutil/decoder.go, internal/xmlutil/rawxml.go, internal/xmlutil/encoder.go, internal/xmlutil/xmlutil_test.go</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md (D-05, D-06, D-08), .planning/phases/01-foundation/01-RESEARCH.md (Patterns 1–3, safe decoder + canonical emission code examples, §Conformance, LOC budget, Pitfall 7)</read_first>
  <behavior>
    - Registry.PrefixFor(wml-main-URI) returns "w"; Strict purl.oclc.org URIs map to the same prefixes as Transitional
    - NewSafeDecoder: Strict=true, Entity=nil, rejects xml.Directive (DOCTYPE) via ErrDOCTYPE sentinel; depth >512 → ErrXMLDepth
    - RawXML round-trips a mixed-namespace subtree verbatim, preserving attribute order (token-copy, no map re-serialization)
    - Encoder emits `<w:p>` not `<p xmlns="…">`; root carries all declared xmlns:* once; mc:Ignorable lists extension prefixes
    - Billion-laughs style DOCTYPE input returns clean error, never expansion
  </behavior>
  <action>
    ns.go: `Registry` with bidirectional URI↔prefix maps; `canonical` table per RESEARCH (w, r, a, wp, wp14, mc, w14, w15, plus package/content-types/rels URIs and both Transitional + Strict purl.oclc.org URI families → same prefixes per D-08); funcs `PrefixFor(uri string) string`, `NormalizeURI(uri string) string` (Strict→Transitional), URI constants exported.
    decoder.go: `NewSafeDecoder(r io.Reader, maxBytes int64) *xml.Decoder` (Strict, no Entity, LimitReader); `RejectDirective(tok xml.Token) error` returning wrapped `ErrDOCTYPE`; `CheckDepth` helper or depth-tracking token wrapper returning wrapped `ErrXMLDepth` at 512.
    rawxml.go: `RawXML struct{ Tokens []xml.Token }` implementing `xml.Marshaler` and `xml.Unmarshaler` exactly per RESEARCH Pattern 3 (xml.CopyToken every token, depth counter); marshal replays tokens verbatim (no prefix canonicalization inside RawXML — unknown subtrees carry own xmlns).
    encoder.go: `Encoder` wrapping `xml.Encoder`: `EncodeToken` rewrites `xml.Name{Space:URI}` to `prefix:local` with Space cleared via Registry; first root start-element receives synthesized `xmlns:*` attributes for every used prefix plus `mc:Ignorable` ("w14 w15 wp14"); `EncodeElement`/`Encode` delegate accordingly.
    Keep total ≤ ~550 LOC (RESEARCH budget); if mc:AlternateContent canonicalization threatens budget, keep such subtrees inside RawXML (raw replay fallback).
    Write tests FIRST (tdd): table-driven tests over registry, decoder safety (billion-laughs fixture string → ErrDOCTYPE; depth bomb → ErrXMLDepth), RawXML round-trip with attribute-order assertion, encoder golden: marshal small struct → output contains `<w:p` and exactly one `xmlns:w=` occurrence on root, zero per-element redeclarations.
  </action>
  <verify>
    <automated>go test ./internal/xmlutil/ -count=1 && go vet ./internal/xmlutil/</automated>
  </verify>
  <acceptance_criteria>
    - TestRegistry: both Transitional and Strict WML-main URIs return prefix "w"
    - TestSafeDecoder: input containing `<!DOCTYPE` returns error matching ErrDOCTYPE; 600-deep nesting returns ErrXMLDepth; valid shallow XML parses
    - TestRawXML: subtree `<foo:a xmlns:foo="urn:x" b="1" c="2"><foo:b/></foo:a>` replays byte-exact including attribute order b before c
    - TestEncoder: golden output starts with root element declaring `xmlns:w=` exactly once in document; no `xmlns="http://schemas.openxmlformats.org/wordprocessingml` default-namespace form anywhere in output
    - go vet clean; package ≤ ~800 LOC (RESEARCH flag threshold)
  </acceptance_criteria>
  <done>xmlutil passes all tests; canonical-prefix emission proven by goldens; OPC-03 machinery and WML-04 hoarding primitive ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: wml — ~62 essential types with URI tags, whitespace fidelity, RawXML hoards</name>
  <files>internal/wml/namespaces.go, internal/wml/document.go, internal/wml/properties.go, internal/wml/styles.go, internal/wml/numbering.go, internal/wml/table.go, internal/wml/wml_test.go</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md (D-05..D-08), .planning/phases/01-foundation/01-RESEARCH.md (§WML Type List — the 62-type breakdown, CT_Text whitespace code example, Pitfalls 1/2/3), internal/xmlutil/rawxml.go</read_first>
  <behavior>
    - Each CT_* type: unmarshal XML fixture snippet → marshal via xmlutil.Encoder → byte-equal round-trip
    - CT_Text with leading/double spaces: xml:space="preserve" present after marshal; clean text has no xml:space attr
    - Unknown element inside w:pPr lands in Raw []xmlutil.RawXML and re-emits verbatim
    - Non-canonical prefix input (`<word:p xmlns:word="…main">`) parses into CT_P identically to `w:` input
  </behavior>
  <action>
    namespaces.go: export URI consts `NSWMLMain`, `NSRels`, `NSXMLSpace`, etc., referencing xmlutil constants.
    Implement the 62-type list from RESEARCH §WML Type List verbatim in scope (D-guided): document.go (CT_Document, CT_Body, CT_HdrFtr + Hdr/Ftr roots, CT_P, CT_PPr, CT_R, CT_RPr, CT_Text, CT_Br, CT_Tab, CT_Cr, CT_SoftHyphen, CT_NoBreakHyphen, CT_SectPr, CT_PgSz, CT_PgMar, CT_Cols, CT_DocGrid, CT_HdrFtrRef); properties.go (CT_PStyle, CT_Jc, CT_Spacing, CT_Ind, CT_NumPr+CT_NumId+CT_ILvl, CT_KeepNext/KeepLines/PageBreakBefore/WidowControl as CT_OnOff fields, CT_Tabs+CT_TabStop, CT_RStyle, CT_RFonts, CT_B/CT_I via CT_OnOff, CT_U, CT_Sz, CT_Color, CT_Highlight, CT_VertAlign, CT_Lang, CT_Strike/CT_DStrike, CT_Vanish, CT_Kern, CT_OnOff, CT_Shd); styles.go (CT_Styles, CT_Style, CT_StyleName, CT_BasedOn, CT_Next, CT_Link, CT_DocDefaults, CT_RPrDefault, CT_PPrDefault, CT_LatentStyles, CT_LsdException); numbering.go (CT_Numbering, CT_Num, CT_AbstractNum, CT_Lvl, CT_NumFmt, CT_LvlText, CT_Start); table.go (CT_Tbl, CT_TblPr, CT_TblStyle, CT_TblW, CT_TblBorders+CT_TblBorder, CT_TblGrid+CT_GridCol, CT_Tr, CT_TrPr, CT_Tc, CT_TcPr, CT_GridSpan, CT_VMerge); settings root CT_Settings + CT_Zoom, CT_DefaultTabStop, CT_Compat+CT_CompatSetting in properties.go or a settings block in styles.go (agent's discretion); CT_Theme as typed shell with RawXML body.
    ALL struct tags use `xml:"<URI> <local>"` form including namespaced attributes (D-05; e.g. w:val → `xml:"<NSWMLMain> val,attr"`); optional fields are pointers, nil=absent (D-07); container structs (Body, P, R, PPr, RPr, Tbl, Tr, Tc, SectPr, Style) carry `Raw []xmlutil.RawXML \`xml:",any"\`` (D-06).
    CT_Text: `Space *string` with xml-URI attr tag + `Value string \`xml:",chardata"\``; custom MarshalXML sets xml:space="preserve" iff `strings.TrimSpace(v) != v || strings.Contains(v, "  ")` (WML-03). Never merge runs.
    Tests: per-file round-trip table tests (unmarshal→marshal→compare bytes through xmlutil.Encoder); prefix-agnostic test parsing `<word:p>` variant; whitespace tests for the three CT_Text cases; hoard test with unknown `<w14:paraId>`-style element in pPr asserting verbatim re-emission.
  </action>
  <verify>
    <automated>go test ./internal/wml/ ./internal/xmlutil/ -count=1 && go vet ./internal/wml/</automated>
  </verify>
  <acceptance_criteria>
    - Exported CT_* type count across internal/wml is between 55 and 70 (`go doc ./internal/wml | grep -c '^type CT_'` in range)
    - Every round-trip test byte-equal after marshal through xmlutil.Encoder
    - TestWhitespace: "  hello" marshals with `xml:space="preserve"`; "hello" marshals without any xml:space attribute; adjacent runs never merged (2 runs in → 2 runs out)
    - TestPrefixAgnostic: `<word:p xmlns:word="http://schemas.openxmlformats.org/wordprocessingml/2006/main"/>` unmarshals into CT_P with no error
    - TestHoard: unknown child under CT_PPr round-trips byte-identical
    - go vet clean
  </acceptance_criteria>
  <done>WML-01..04 satisfied: ~62 types, bidirectional marshal with canonical prefixes, whitespace fidelity, unknown-child hoarding — all test-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| XML bytes→Decoder | Untrusted part XML crosses into encoding/xml token stream |
| model→Encoder | Serialized output must not inject namespace smuggling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | DoS | xmlutil decoder (entity expansion / billion laughs) | high | mitigate | Strict decoder, Entity=nil, reject xml.Directive (DOCTYPE) → ErrDOCTYPE; billion-laughs unit test mandatory (OPC-07) |
| T-01-07 | DoS | deep XML nesting | medium | mitigate | Token-depth cap 512 → ErrXMLDepth |
| T-01-08 | Tampering | prefix/namespace confusion on write | medium | mitigate | Canonical-prefix encoder emits declared prefixes only; unknown URIs inside RawXML replay verbatim with own xmlns, never remapped |
</threat_model>

<verification>
- `go test ./internal/xmlutil/ ./internal/wml/ -count=1` exits 0
- Encoder golden tests prove canonical prefixes (Pitfall 2 regression guard)
- Prefix-agnostic parse test proves OPC-03 read behavior
</verification>

<success_criteria>
OPC-03 (URI-based resolution, prefix-agnostic), WML-01 (~62 types), WML-02 (bidirectional, canonical), WML-03 (xml:space fidelity, no run merging), WML-04 (RawXML hoarding) all green under go test.
</success_criteria>

## Artifacts this phase produces

- Package `internal/xmlutil`:
  - Types: `Registry`, `Encoder`, `RawXML`
  - Functions: `NewSafeDecoder(r io.Reader, maxBytes int64) *xml.Decoder`, `RejectDirective(xml.Token) error`, `(Registry).PrefixFor(uri string) string`, `NormalizeURI(uri string) string`
  - Sentinels: `ErrDOCTYPE`, `ErrXMLDepth` (shared with opc)
  - URI consts for WML/Rels/DrawingML/wp/mc/w14/w15 Transitional + Strict families
- Package `internal/wml`:
  - ~62 exported `CT_*` types (see Task 2 action for full list by file)
  - Consts: `NSWMLMain`, `NSRels`, `NSXMLSpace`
  - `CT_Text` with whitespace-fidelity MarshalXML
- Files: `internal/xmlutil/{ns,decoder,rawxml,encoder}.go`, `internal/xmlutil/xmlutil_test.go`, `internal/wml/{namespaces,document,properties,styles,numbering,table}.go`, `internal/wml/wml_test.go`

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
