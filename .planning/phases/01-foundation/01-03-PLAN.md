---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - wordingo.go
  - create.go
  - defaults/styles.xml
  - defaults/theme1.xml
  - defaults/fontTable.xml
  - defaults/settings.xml
  - create_test.go
autonomous: false
requirements: [CREATE-01, CREATE-02]
user_setup:
  - service: microsoft-word
    why: "Manual open-without-repair gate (D-01) — CREATE-02 cannot be verified programmatically on this machine"
must_haves:
  truths:
    - wordingo.Create() returns a document whose saved bytes form a valid OPC package in canonical order
    - Blank document contains Normal + Heading 1–9 + Title styles, default theme, font table, settings, one sectPr
    - Output opens in Word 2016/2019/2021/M365 and LibreOffice with no repair dialog (manual gate, D-01)
    - document.xml + sectPr are generated through the wml/xmlutil write path (exercises CREATE-01 end-to-end)
  artifacts:
    - wordingo.go (public package shell, Create(), Warnings())
    - create.go (blank assembly over internal/opc)
    - defaults/*.xml embedded static blobs (styles, theme1, fontTable, settings)
  key_links:
    - go:embed static default parts authored once against real Word blank output
    - opc.Save canonical ordering + deterministic headers produce the final package
    - sectPr built from wml structs (Letter 12240×15840, 1440 margins, cols, docGrid)
---

<objective>
Deliver the public `wordingo` package shell with `Create()`: a blank .docx generator assembling embedded default parts (styles, theme, fontTable, settings) plus a struct-generated document.xml+sectPr, packaged via internal/opc. Gated by a manual Word-open checkpoint (D-01, CREATE-02).

Purpose: Proves the full write path end-to-end and satisfies the phase thesis "Word opens without repair".
Output: wordingo.go, create.go, defaults/ embedded XML, create_test.go.
</objective>

<execution_context>
@/Users/fabiomarini/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/fabiomarini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PRD.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Embedded default parts + Create() assembly</name>
  <files>wordingo.go, create.go, defaults/styles.xml, defaults/theme1.xml, defaults/fontTable.xml, defaults/settings.xml, create_test.go</files>
  <read_first>.planning/phases/01-foundation/01-CONTEXT.md (D-01, D-09, D-12), .planning/phases/01-foundation/01-RESEARCH.md (§Blank Document Part Set — exact part list, recommended construction, heading style essentials), .planning/PRD.md §FR-4 + API sketch, internal/opc/package.go, internal/wml/document.go</read_first>
  <behavior>
    - Create() then Save() produces zip whose first two entries are [Content_Types].xml and _rels/.rels
    - Package contains exactly: [Content_Types].xml, _rels/.rels, word/document.xml, word/_rels/document.xml.rels, word/styles.xml, word/settings.xml, word/fontTable.xml, word/theme/theme1.xml (webSettings + docProps omitted per RESEARCH A3)
    - styles.xml contains w:style entries for Normal (default), heading 1–9, Title
    - document.xml body has one sectPr: pgSz w=12240 h=15840, pgMar 1440 all sides, cols, docGrid
    - Reopening saved output via opc.Open round-trips with zero errors and empty Warnings
  </behavior>
  <action>
    defaults/: author four static XML blobs per RESEARCH §Blank Document Part Set — styles.xml (docDefaults Calibri 11pt sz=22 with theme fonts; latentStyles; Normal default; Heading1–9 each with w:name "heading N", basedOn Normal, next Normal, qFormat, uiPriority, keepNext, spacing, outlineLvl 0–8, rFonts asciiTheme="majorHAnsi", theme color accent1, sz ramp 32→~18; Title sz=52), theme1.xml (standard Office a:theme clrScheme/fontScheme/fmtScheme blob), fontTable.xml (Calibri, Calibri Light, Cambria with panose/sig), settings.xml (zoom 100, defaultTabStop 720, no compat). If `testdata/word/blank.docx` fixture exists, extract its part values to minimize diff (A4); otherwise use RESEARCH values. Embed via `//go:embed defaults`.
    wordingo.go: public package `wordingo`; type `Document` wrapping `*opc.Package`; `Create() *Document`; `Save(w io.Writer) error`; `SaveFile(path string) error` convenience; `Warnings() []string` (D-12); `X()` escape-hatch accessor returning underlying internals per PRD sketch.
    create.go: `newBlankPackage()`: build document.xml from wml structs — CT_Document→CT_Body→CT_SectPr{PgSz{W:12240,H:15840}, PgMar{Top:1440,Right:1440,Bottom:1440,Left:1440}, Cols, DocGrid} — marshal via xmlutil.Encoder (canonical prefixes); register all parts into a fresh opc.Package with correct ContentTypes overrides (document main, styles, settings, fontTable, theme) and relationships (_rels/.rels → word/document.xml officeDocument; document.xml.rels rId1..4 → styles, settings, fontTable, theme1); Transitional conformance always on Create (OPC-05).
    create_test.go: build, save to buffer, reopen with opc.Open; assert entry order, part set equality with expected list, styles content markers (`val="heading 1"` … `val="heading 9"`, `val="Title"`, `w:styleId="Normal"`), sectPr attribute values, zero warnings, empty-body validity.
  </action>
  <verify>
    <automated>go test . -run 'TestCreate' -count=1 && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - TestCreate: saved zip entry[0]=="[Content_Types].xml", entry[1]=="_rels/.rels"
    - Part set equals the 8-part expected list exactly (no webSettings, no docProps)
    - styles.xml contains all of `heading 1`, `heading 9`, `Title`, and a default Normal style
    - document.xml contains `w:w="12240"` and `w:h="15840"` and `w:top="1440"` in sectPr
    - document.xml root declares canonical prefixes (`xmlns:w=`), produced via xmlutil.Encoder
    - Reopen round-trip: opc.Open succeeds, Warnings() length 0
    - go vet ./... clean
  </acceptance_criteria>
  <done>Create() generates the full minimal blank package; all structural tests pass; ready for manual Word gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual Word/LibreOffice open gate (CREATE-02, D-01)</name>
  <files>.planning/tmp/blank.docx</files>
  <action>Executor regenerates .planning/tmp/blank.docx via the create_test.go helper, then hands to user for the manual open check.</action>
  <verify>User opens .planning/tmp/blank.docx in Word and LibreOffice; no repair dialog in either</verify>
  <done>User approves: blank.docx opens clean in Word (no repair) and LibreOffice</done>
  <what-built>wordingo.Create() blank document generator; test helper `TestMain`-adjacent utility or `go run ./cmd/…` NOT included — instead executor writes `blank-output.docx` via a small `create_test.go` helper invocation (`go test -run TestCreateWritesGolden -v` regenerates `.planning/tmp/blank.docx`).</what-built>
  <how-to-verify>
    1. Run: `go test . -run TestCreate -count=1` (all green).
    2. Generate artifact: executor produces `.planning/tmp/blank.docx` via the test helper.
    3. Open `.planning/tmp/blank.docx` in Microsoft Word (2016/2019/2021/M365 — any available): expect NO repair dialog, blank page renders, Normal + Heading 1–9 + Title visible in Styles gallery.
    4. Open same file in LibreOffice Writer: expect clean open, no errors.
    5. If Word reports a problem: paste the exact dialog text / validation detail back as the resume message — likely missing part (RESEARCH A3: add webSettings.xml or docProps) or malformed styles value (A4: copy exact values from your Word blank fixture).
  </how-to-verify>
  <resume-signal>Type "approved" (opens clean in Word + LibreOffice) or describe the repair/validation message</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| embedded blobs→package | Static XML shipped in binary — trust limited to build-time |
| generated docx→Word | Output consumed by strict external validators |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Tampering | embedded default XML blobs | low | mitigate | Blobs are compile-time constants via go:embed; verified by structural tests (part set, style markers) |
| T-01-10 | DoS | Create() resource use | low | accept | Bounded: 8 small static parts + one generated part; no user input in Phase 1 create path |
</threat_model>

<verification>
- `go build ./... && go vet ./... && go test ./... -count=1` all exit 0
- Manual gate (Task 2) approved: Word + LibreOffice open blank.docx without repair
</verification>

<success_criteria>
CREATE-01 (full default part set via Create()) and CREATE-02 (Word-open gate approved) satisfied. Phase 1 thesis proven.
</success_criteria>

## Artifacts this phase produces

- Package `wordingo` (repo root):
  - Types: `Document`
  - Functions: `Create() *Document`, `(*Document).Save(w io.Writer) error`, `(*Document).SaveFile(path string) error`, `(*Document).Warnings() []string`, `(*Document).X()`
- Files: `wordingo.go`, `create.go`, `create_test.go`
- Embedded blobs: `defaults/{styles,theme1,fontTable,settings}.xml`

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
