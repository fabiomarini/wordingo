---
phase: 02-style-engine
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/style/errors.go
  - internal/style/cloner.go
  - internal/style/cloner_test.go
  - internal/style/corpus_test.go
  - testdata/word/style-rich/heading-chain.expected.json
  - testdata/word/style-rich/theme-refs.expected.json
  - testdata/word/style-rich/multi-level-numbering.expected.json
  - testdata/style-engine/hostile/circular-basedon.expected.json
  - testdata/style-engine/hostile/dangling-basedon.expected.json
  - testdata/style-engine/hostile/missing-numid.expected.json
autonomous: true
requirements: [STYLE-CLONE-01, STYLE-CLONE-02]
user_setup:
  - service: word-desktop
    why: "Authoring real .docx style-rich fixtures (heading-chain, theme-refs, multi-level-numbering) and hostile .docx fixtures (circular-basedon, dangling-basedon, missing-numid) in Word. The executor delivers synthetic in-memory equivalents for CI + the expected.json schema files; the real .docx files require Word on a desktop and are user-authored per Phase 1 D-02."
    artifacts:
      - "testdata/word/style-rich/heading-chain.docx"
      - "testdata/word/style-rich/theme-refs.docx"
      - "testdata/word/style-rich/multi-level-numbering.docx"
      - "testdata/style-engine/hostile/circular-basedon.docx"
      - "testdata/style-engine/hostile/dangling-basedon.docx"
      - "testdata/style-engine/hostile/missing-numid.docx"

must_haves:
  truths:
    - "CloneStyles(src, dst) byte-copies all 5 source parts (styles.xml, numbering.xml, fontTable.xml, theme/theme1.xml, settings.xml) into dst via opc.MarkModified — no re-parse, no transformation (per D-09)"
    - "CloneStyles allocates a fresh rId in dst's word/document.xml relationship set per cloned part via Relationships.NextRID and adds a Relationship with the canonical OOXML relationship type URI for each part (per D-09, OPC-06)"
    - "CloneStyles registers a content-type Override for each cloned part in dst.ContentTypes.Overrides keyed by the OPC-absolute part name with leading slash (per PATTERNS critical flag on ContentTypes.Overrides)"
    - "CloneStyles returns ErrCloneTargetNotEmpty when dst already has any of the 5 style parts — fresh-empty-target only (per D-08)"
    - "CloneStyles skips a source part that is absent from src (source may legitimately lack numbering.xml or fontTable.xml) without error — clone is best-effort over present parts"
    - "A cloned target package round-trips through opc.Save and the 5 cloned parts are byte-pass-through-identical to the source (per D-09, OPC-04)"
    - "Named styles from the cloned template are resolvable by styleId through the 02-01 resolver running over the cloned target — STYLE-CLONE-02 (cloner preserves styles.xml verbatim, resolver exposes by styleId)"
    - "The Heading2 chain fixture (Heading2→Heading1→Normal) resolves identical effective CT_PPr/CT_RPr to the values listed in heading-chain.expected.json (ROADMAP success criterion #4)"
    - "Circular basedOn (A→B→A), dangling basedOn ref, and missing numId surface via Warnings() containing the expected substrings recorded in the hostile expected.json files (per D-05, D-07)"
    - "Real .docx fixtures absent from testdata/ cause validation tests to t.Skip with a clear message; synthetic in-memory equivalents cover the same assertion paths in CI (per Phase 1 D-02 fixture-availability pattern)"
  artifacts:
    - "internal/style/errors.go declares var ErrCloneTargetNotEmpty = errors.New(\"style: clone target not empty\")"
    - "internal/style/cloner.go defines CloneStyles(src, dst *opc.Package) error and the cloneParts constant table (5 entries: name, relType, ct)"
    - "internal/style/cloner_test.go covers: byte-pass-through-identity for all 5 parts, ErrCloneTargetNotEmpty on non-empty target, fresh-empty-target success, absent-source-part skip, relationship rId allocation, content-type Override registration, opc.Save round-trip"
    - "internal/style/corpus_test.go defines buildSyntheticStyleRichZip helpers (heading-chain, theme-refs, multi-level-numbering, circular-basedon, dangling-basedon, missing-numid), the expected.json loader, the effective-props equality assertion helper, and the t.Skip-guarded real-fixture hooks"
    - "testdata/word/style-rich/{heading-chain,theme-refs,multi-level-numbering}.expected.json exist with the schema documented in 02-RESEARCH §Fixture Design"
    - "testdata/style-engine/hostile/{circular-basedon,dangling-basedon,missing-numid}.expected.json exist with the hostile-schema (expected props + warnings array)"
  key_links:
    - "CloneStyles calls dst.MarkModified(partName, srcBytes) per source part — the entire byte pass-through path (D-09); no re-parse, no wml decode in the cloner"
    - "CloneStyles calls dst.ContentTypes.Overrides[\"/\"+partName] = ct per part — PATTERNS critical flag: the Overrides key carries a leading slash; reconcile with the constant table that uses package-relative names without slash"
    - "CloneStyles calls dst.Rels[\"word/document.xml\"].NextRID() and appends Relationship{ID:rid, Type:relType, Target:partName} per part — relationship graph wiring (OPC-06)"
    - "corpus_test.go exercises the 02-01 resolver (NewResolver, ResolveParagraph, ResolveRun, Warnings) over cloned synthetic packages — the cloner→resolver integration point for STYLE-CLONE-02"
    - "corpus_test.go exercises the 02-02 post-processors (theme color concretization, numbering level pPr merge) over cloned synthetic packages containing theme1.xml and numbering.xml — the cloner→resolver→theme/numbering integration"
---

<objective>
Build the dependency-graph cloner (`internal/style/cloner.go` + `ErrCloneTargetNotEmpty` sentinel) and the real-template test corpus validation harness (`internal/style/corpus_test.go` + `testdata/word/style-rich/*.expected.json` + `testdata/style-engine/hostile/*.expected.json`).

Purpose: STYLE-CLONE-01 (copy template style dependency graph into target document) and STYLE-CLONE-02 (named styles from template applicable to new content by name). The cloner treats the 5 source parts as raw bytes via OPC pass-through (D-09) and refuses non-empty targets (D-08). The corpus validation harness proves the cloner + 02-01 resolver + 02-02 theme/numbering post-processors produce correct effective props against hand-authored expected-values JSON, covering the canonical Heading2 chain (ROADMAP success criterion #4), theme color refs, multi-level numbering, and the D-05/D-07 hostile paths (circular basedOn, dangling basedOn, missing numId).

Output: `internal/style/cloner.go`, `internal/style/cloner_test.go`, `internal/style/corpus_test.go`, extended `internal/style/errors.go` (adds `ErrCloneTargetNotEmpty` to the 02-01/02-02 sentinel file), and 6 `expected.json` schema files under `testdata/`.

**Wave structure (two task groups):** Task 1 runs in Wave 1 (no dependencies — only touches `internal/style/errors.go`, `cloner.go`, `cloner_test.go`; `errors.go` is shared with 02-01/02-02 but the addition is a new sentinel, no field conflict). Task 2 runs in Wave 2 (depends_on 02-01 + 02-02 — the corpus validation exercises `Resolver.ResolveParagraph`/`ResolveRun`, `themeCache.ResolveColor`, and `numberingCache.ResolveLvl` over cloned packages, so the resolver + theme + numbering must exist first). The frontmatter `wave: 1` and `depends_on: []` reflect Task 1 (the cloner). Task 2 carries inline `wave: 2` / `depends_on: [02-01, 02-02]` notes in its `<name>` and `<action>` for the executor and orchestrator.
</objective>

<execution_context>
@/Users/fabiomarini/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/fabiomarini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRD.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-style-engine/02-CONTEXT.md
@.planning/phases/02-style-engine/02-RESEARCH.md
@.planning/phases/02-style-engine/02-PATTERNS.md

# Phase 1 OPC layer the cloner composes (no new opc code in this plan)
@internal/opc/package.go
@internal/opc/relationships.go
@internal/opc/contenttypes.go
@internal/opc/errors.go
@internal/opc/opc_test.go

# Reference defaults — what a complete style dependency graph looks like
@defaults/styles.xml
@defaults/theme1.xml
@defaults/fontTable.xml
@defaults/settings.xml

# Upstream 02-01 + 02-02 plans — Task 2 (corpus validation) requires their artifacts
@.planning/phases/02-style-engine/02-01-PLAN.md
@.planning/phases/02-style-engine/02-02-PLAN.md

# 02-01 + 02-02 outputs — Task 2 exercises these over cloned packages
@internal/style/resolver.go
@internal/style/theme.go
@internal/style/numbering.go
@internal/style/errors.go
</context>

<phase_boundary>
## Scope boundary with 02-01 (upstream) and 02-02 (upstream)

**This plan does NOT:**

- Modify the 02-01 resolver (`resolveChainPPr`, `resolveChainRPr`, `collectChain`, `findStyle`, `findLatent`, `mergePPr`, `mergeRPr`, `mergeSpacing`, `mergeInd`, `mergeRFonts`, `mergeColor`, `clonePPr`, `cloneRPr`, `ensureStylesParsed`, `invalidateCacheIfStale`, `ResolveParagraph`, `ResolveRun`, `Warnings`, `NewResolver`). Task 2 *calls* the resolver over cloned packages but does not edit `resolver.go`.
- Modify the 02-02 theme/numbering layers (`themeCache`, `themeEnumToElement`, `ResolveColor`, `applyTint`/`applyShade`/`parseHexRGB`/`clampByte`, `numberingCache`, `ResolveLvl`, `findNum`/`findAbstractNum`/`findLvl`/`findOverride`, `invalidateIfStale`). Task 2 *exercises* them via `ResolveParagraph`/`ResolveRun` but does not edit `theme.go` or `numbering.go`.
- Modify the 02-02 `CT_Num.LvlOverride` struct extension in `internal/wml/numbering.go`. The cloner byte-copies `numbering.xml` (D-09 — no re-parse); the struct extension only governs the lazy resolver parse path, which is 02-02's concern.
- Implement merge-by-styleId clone policy. D-08 is fresh-empty-target only; merging is a deferred idea (CONTEXT.md `<deferred>`). The cloner returns `ErrCloneTargetNotEmpty` for any pre-existing style part in the target — no partial merge.
- Implement `FromTemplate()` (CREATE-03) or `Document` open/read/save. Those are Phase 3. The cloner is a primitive operating on `*opc.Package`; Phase 3's `Document.FromTemplate` will call `CloneStyles` under the hood.
- Concretize theme colors or merge numbering level pPr in the cloner. The cloner never parses the 5 parts (D-09). Theme/numbering post-processing happens in the 02-01/02-02 resolver at resolve time, not at clone time. Invalid source surfaces at resolve time as a warning (D-07), not at clone time.

**Backward compatibility contract:**

The `ErrCloneTargetNotEmpty` sentinel is added to `internal/style/errors.go`, which 02-01 (`ErrStylesParseFailed`) and 02-02 (`ErrThemeParseFailed`, `ErrNumberingParseFailed`) already extended. The addition is purely additive (new `var` declaration); no existing sentinel is renamed or retyped. All 02-01 and 02-02 tests must still pass.
</phase_boundary>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 1, depends_on []): Build internal/style/cloner.go + ErrCloneTargetNotEmpty sentinel — byte pass-through clone of the 5 style parts</name>
  <files>internal/style/errors.go, internal/style/cloner.go, internal/style/cloner_test.go</files>
  <read_first>
    - internal/opc/package.go (full file — Part struct:40-47, Open:51-63, Package struct:95-104, Warnings:108, MarkModified:113-121, MarkDeleted:124-128, Open:134-216, readAllCapped:219-230 unexported, Save:258-335, validatePartName:352-380)
    - internal/opc/relationships.go (full file — Relationship struct:18-23, Relationships struct:28-31, NextRID:138-145 high-water allocator, Add pattern via append to rs.Rels, validate:161-191 enforces unique ids + dangling targets on Save)
    - internal/opc/contenttypes.go (full file — ContentTypes struct:17-20 with Defaults + Overrides maps, PATTERNS critical flag: Overrides key is OPC-absolute with leading "/" — TypeFor:60-75 reads `c.Overrides["/"+partName]`, serialize:80-108)
    - internal/opc/errors.go (full file — sentinel pattern + %w wrapping convention, D-11)
    - internal/opc/opc_test.go (buildSyntheticZip:47-73 fixture builder pattern to copy into cloner_test; DiffParts byte-diff helper if present; errors.Is assertion:240-242; HasFixtures t.Skip pattern:86-95)
    - defaults/styles.xml, defaults/theme1.xml, defaults/fontTable.xml, defaults/settings.xml (reference for the 5-part dependency graph; confirm part names: word/styles.xml, word/theme/theme1.xml, word/fontTable.xml, word/settings.xml — no numbering.xml in defaults/)
    - .planning/phases/02-style-engine/02-RESEARCH.md §Pattern 5 (lines 342-381 — CloneStyles reference implementation + cloneParts constant table), §Don't Hand-Roll (lines 400-409 — reuse MarkModified/NextRID/ContentTypes), §Common Pitfall 4 (CT_Num.LvlOverride struct extension is 02-02's job; cloner is byte pass-through, unaffected), §Architectural Responsibility Map (rows: byte-copy 5 parts, rId allocation, content-type overrides — all primary tier internal/style cloner, secondary tier internal/opc)
    - .planning/phases/02-style-engine/02-PATTERNS.md §"internal/style/cloner.go" (lines 60-158 — analogs: MarkModified, NextRID, ContentTypes.Overrides; critical flag on leading-slash key at line 129; Part-name + content-type table lines 146-155; target-empty precondition D-08; error wrapping convention; path-traversal safety inherited from opc)
    - .planning/phases/02-style-engine/02-CONTEXT.md D-08 (fresh-empty-target only, ErrCloneTargetNotEmpty), D-09 (5 parts as raw bytes via OPC pass-through, no re-parse), D-07 (invalid source surfaces at resolve time not clone time — cloner does not validate XML)
    - .planning/phases/02-style-engine/02-01-PLAN.md (confirm errors.go already declares ErrStylesParseFailed — this task extends the same file, does not create a new one)
    - .planning/phases/02-style-engine/02-02-PLAN.md (confirm errors.go already declares ErrThemeParseFailed + ErrNumberingParseFailed — this task adds ErrCloneTargetNotEmpty to the same file)
  </read_first>
  <behavior>
    - Fresh-empty-target success: CloneStyles(src with all 5 parts, dst with none of the 5 parts) returns nil; dst.Parts contains all 5 part names; each dst part's data equals the corresponding src part's bytes (byte pass-through, D-09).
    - Non-empty target error: dst already has word/styles.xml → CloneStyles returns an error that wraps ErrCloneTargetNotEmpty via fmt.Errorf("style: clone %s: %w", name, ErrCloneTargetNotEmpty); errors.Is(err, ErrCloneTargetNotEmpty) is true (D-08). The error is returned BEFORE any byte copy — no partial clone is left in dst (atomicity: check all 5 preconditions first, then copy).
    - Absent source part skip: src lacks word/numbering.xml (legitimate — defaults/ has no numbering.xml either) → CloneStyles skips that part silently (no error, no warning, no relationship added for it); the other 4 parts are still cloned. The cloner does NOT warn on absent source parts because a template without numbering.xml is a valid Word document.
    - Relationship allocation: after CloneStyles, dst.Rels["word/document.xml"] contains 5 (or fewer if source lacked parts) new Relationship entries, each with a unique rId allocated by NextRID (high-water, no reuse per OPC-06), the canonical OOXML relationship type URI, and Target equal to the part name (package-relative, no leading slash — Relationship.Target is relative, not OPC-absolute).
    - Content-type Override registration: after CloneStyles, dst.ContentTypes.Overrides["/word/styles.xml"] == "application/vnd.openxmlformats-officedocument.wordprocessingml.styles+xml" (and likewise for the other 4 parts) — the leading slash is present (PATTERNS critical flag).
    - Round-trip byte identity: after CloneStyles + opc.Save(dst) → re-open the saved bytes → the 5 cloned parts are byte-identical to the source's 5 parts (opc.Save raw-copies modified parts via writeDeterministic, but the payload bytes are the src bytes passed through MarkModified — identity holds at the payload level).
    - No re-parse: the cloner never calls xmlutil.NewSafeDecoder or wml.CT_Styles/CT_Numbering/CT_Theme — it operates purely on []byte via MarkModified (D-09).
    - No fontTable consultation: the cloner copies fontTable.xml as raw bytes; it does not read font names from it (Pitfall 6 boundary — fontTable is metadata, not a name lookup).
    - Path traversal safety: all 5 part names are compile-time string constants (cloneParts table); no user-supplied names reach MarkModified or ContentTypes.Overrides. Path traversal is structurally impossible in the cloner (mitigated by design, inherited from opc.validatePartName on Open).
    - Idempotent rId: calling CloneStyles twice on the same dst fails on the second call with ErrCloneTargetNotEmpty (the first call populated the parts — D-08 fresh-empty-target policy).
  </behavior>
  <action>
    Extend `internal/style/errors.go` (the 02-01/02-02 sentinel file — do NOT create a new errors file): add `var ErrCloneTargetNotEmpty = errors.New("style: clone target not empty")` immediately after the existing `ErrNumberingParseFailed` declaration (added by 02-02 Task 3). Follow the Phase 1 D-11 sentinel + `%w` wrapping convention from `internal/opc/errors.go`. Add a doc comment citing D-08 (fresh-empty-target only; merge-by-styleId is deferred). This is the last sentinel the style package needs for v1.

    Create `internal/style/cloner.go` with package `style` and imports: `fmt`, `io`, `github.com/fabiomarini/wordingo/internal/opc`. No `encoding/xml`, no `internal/wml`, no `internal/xmlutil` — the cloner does not parse (D-09).

    Define the package-level `cloneParts` constant table exactly as specified in 02-PATTERNS lines 146-155 and 02-RESEARCH Pattern 5 lines 349-359. It is a slice of structs with three string fields: `name` (package-relative part name, no leading slash — e.g. "word/styles.xml"), `relType` (the canonical OOXML relationship type URI — e.g. "http://schemas.openxmlformats.org/officeDocument/2006/relationships/styles"), `ct` (the content-type MIME — e.g. "application/vnd.openxmlformats-officedocument.wordprocessingml.styles+xml"). The 5 entries, in this order: styles.xml, numbering.xml, fontTable.xml, theme/theme1.xml, settings.xml. Use the exact URIs and MIMEs from 02-PATTERNS line 149-153 — copy them verbatim (they are ISO/ECMA-376 canonical). Add a doc comment citing ISO/IEC 29500-1 §15 (Package Relationships) and the 5 OOXML relationship type URIs.

    Define `func CloneStyles(src, dst *opc.Package) error` per 02-RESEARCH Pattern 5 (lines 361-380) and 02-PATTERNS §"internal/style/cloner.go" (lines 60-158). The algorithm:

    Step 1 — Precondition scan (atomicity): iterate `cloneParts`; for each entry, check `if _, exists := dst.Parts[p.name]; exists { return fmt.Errorf("style: clone %s: %w", p.name, ErrCloneTargetNotEmpty) }`. Complete the full scan BEFORE any byte copy — a non-empty target aborts with no side effects on dst. Mirror the mandatory-part guard shape from `internal/opc/package.go:175-178` (the `[Content_Types].xml` existence check).

    Step 2 — Byte copy + relationships + content types: iterate `cloneParts` again; for each entry:
    - `srcPart, ok := src.Parts[p.name]`; if `!ok` → `continue` (absent source part — skip silently; a template without numbering.xml or fontTable.xml is valid).
    - Read source bytes: `rc, err := srcPart.Open()`; `defer rc.Close()`; `bytes, err := io.ReadAll(rc)` (inlined `readAllCapped` — 02-PATTERNS line 107 recommends inlining to avoid touching the opc export surface; the cap is already enforced by `Part.Open`'s `capReader` returning `ErrDecompressionLimit` past `MaxPartBytes`). On error: `return fmt.Errorf("style: read source %s: %w", p.name, err)`.
    - `dst.MarkModified(p.name, bytes)` — the entire byte pass-through path (D-09). No re-parse, no wml decode.
    - `dst.ContentTypes.Overrides["/"+p.name] = p.ct` — the leading slash is the PATTERNS critical flag (02-PATTERNS line 129; `ContentTypes.TypeFor` reads `c.Overrides["/"+partName]` at contenttypes.go:61). Without the slash, `TypeFor` would miss the override and fall through to Defaults, causing `opc.Save` to fail with "part has no content type" at package.go:280.
    - Relationship wiring: `wordRels := dst.Rels["word/document.xml"]`; if `wordRels == nil` { `wordRels = &opc.Relationships{}; dst.Rels["word/document.xml"] = wordRels` } (defensive — a fresh-empty target created by Phase 1 `Create()` may not yet have a word/document.xml rels set; create it on demand). `rid := wordRels.NextRID()` (OPC-06 high-water, no reuse). `wordRels.Rels = append(wordRels.Rels, opc.Relationship{ID: rid, Type: p.relType, Target: p.name})`. Note: `Relationship.Target` is package-relative (no leading slash) — the leading slash is only for `ContentTypes.Overrides` keys; the relationship Target follows OPC's relative-path convention (validated by `resolveTarget` in relationships.go:195-212).

    Step 3 — Return nil on success.

    Add a file-level doc comment on `cloner.go` citing: (a) D-08 (fresh-empty-target only, ErrCloneTargetNotEmpty; merge-by-styleId deferred), (b) D-09 (5 parts as raw bytes via opc.MarkModified pass-through, no re-parse; invalid source surfaces at resolve time via D-07, not clone time), (c) the 5 OOXML relationship type URIs and content-types (ISO/IEC 29500-1 §15), (d) the reuse of Phase 1 opc primitives (MarkModified, NextRID, ContentTypes.Overrides — per Don't Hand-Roll), (e) the path-traversal safety (all part names are compile-time constants; no user-supplied names reach MarkModified or Overrides — inherited from opc.validatePartName on Open).

    Create `internal/style/cloner_test.go` with package `style` (white-box — same package, can access unexported `cloneParts` table for assertion). Build synthetic source + target package helpers by copying the `buildSyntheticZip` pattern from `internal/opc/opc_test.go:47-73` (t.Helper, t.Fatal on error, fixed-timestamp header). Do NOT export helpers from opc (02-PATTERNS line 411 recommends copying to keep opc's export surface unchanged).

    Define `buildSyntheticSourceZip(t *testing.T) []byte` — a ZIP with `[Content_Types].xml`, `_rels/.rels`, `word/document.xml` (empty body), `word/_rels/document.xml.rels`, and all 5 style parts (word/styles.xml, word/numbering.xml, word/fontTable.xml, word/theme/theme1.xml, word/settings.xml) containing minimal valid XML payloads. Use the `defaults/*.xml` files as the content source where convenient (read them at test time via `os.ReadFile` relative to the repo root, or embed minimal XML string literals — the latter is preferred for hermetic tests). For numbering.xml (no defaults/ equivalent), author a minimal `<w:numbering>` with one abstractNum and one num.

    Define `buildSyntheticTargetZip(t *testing.T) []byte` — a fresh-empty target: `[Content_Types].xml` with only Default entries (no Overrides for the 5 style parts), `_rels/.rels`, `word/document.xml` (empty body), `word/_rels/document.xml.rels` (empty Relationships). No style parts present.

    Define `buildSyntheticTargetWithStylesZip(t *testing.T) []byte` — same as target but with a pre-existing `word/styles.xml` (any bytes) to trigger ErrCloneTargetNotEmpty.

    Define a byte-diff helper `assertPartBytesEqual(t *testing.T, src, dst *opc.Package, name string)` — read src and dst part bytes via `Part.Open()` + `io.ReadAll`, compare with `bytes.Equal`, `t.Fatalf` on mismatch with a hex dump of the first differing byte. Copy the shape from `internal/opc/opc_test.go` `DiffParts` if present (02-PATTERNS line 411 references it at lines 101-145 — copy into cloner_test.go).

    Test cases (one `func Test...` per behavior, using `t.Run` subtests where cases share a fixture):
    - TestCloneStyles_FreshEmptyTarget: buildSyntheticSourceZip + buildSyntheticTargetZip → opc.Open both → CloneStyles(src, dst) → assert nil error; assert all 5 parts present in dst.Parts; assertPartBytesEqual for each of the 5 parts.
    - TestCloneStyles_ErrCloneTargetNotEmpty: buildSyntheticSourceZip + buildSyntheticTargetWithStylesZip → opc.Open both → CloneStyles → assert error; `errors.Is(err, ErrCloneTargetNotEmpty)` true; assert NO byte copy happened (dst's pre-existing styles.xml data is unchanged — the atomicity precondition scan aborted before any copy).
    - TestCloneStyles_AbsentSourcePartSkip: build a source zip WITHOUT word/numbering.xml → CloneStyles → assert nil error; assert dst has 4 parts (styles, fontTable, theme1, settings) but NOT numbering.xml; assert no relationship for numbering was added.
    - TestCloneStyles_RelationshipAllocation: after CloneStyles, assert `dst.Rels["word/document.xml"].Rels` has 5 entries; assert each has a unique rId matching `rId<N>` format; assert `NextRID`'s high-water property (collect all rIds, parse N values, assert they are consecutive from 1 or from the target's pre-existing high-water + 1).
    - TestCloneStyles_ContentTypeOverrides: after CloneStyles, assert `dst.ContentTypes.Overrides["/word/styles.xml"]` equals the styles MIME; same for the other 4 parts. The leading slash is present in every key (PATTERNS critical flag regression guard).
    - TestCloneStyles_SaveRoundTrip: after CloneStyles, call `opc.Save(dst)` to a bytes.Buffer; re-open the saved bytes via `opc.Open`; assert the 5 parts are byte-identical to the source's 5 parts (round-trip identity, D-09 + OPC-04).
    - TestCloneStyles_NoReParse: assert the cloner never touches `internal/xmlutil` or `internal/wml` — this is a structural assertion via `grep -v '^//' internal/style/cloner.go | grep -c 'xmlutil\|wml\.'` == 0 (the action body must not introduce these imports; the gate is enforced in `<done>`).
    - TestCloneStyles_IdempotentFails: call CloneStyles twice on the same dst → second call returns ErrCloneTargetNotEmpty (the first call populated the parts).
    - TestCloneStyles_NilPackageDefensive: CloneStyles(nil, dst) or CloneStyles(src, nil) — assert the cloner returns a clear error (e.g. `fmt.Errorf("style: clone: %w", opc.ErrInvalidPackage)` or a style-specific nil-guard error) rather than panicking. Add a no-panic recover guard mirroring `opc_test.go:573-583`.

    Add a `t.Skip`-guarded placeholder `TestCloneStyles_RealFixtures` that calls a `hasRealStyleRichFixtures()` helper (mirror `opc_test.go:86-95` `HasFixtures`) checking for `testdata/word/style-rich/heading-chain.docx`; skips if absent. This signals where Task 2's real-fixture validation hooks in.

    Test command: `go test ./internal/style/... -count=1 -run Clone -v`.
  </action>
  <verify>
    <automated>cd /Users/fabiomarini/Development/go/wordingo && go build ./internal/style/... && go vet ./internal/style/... && go test ./internal/style/... -count=1 -run Clone -v</automated>
  </verify>
  <done>
    - `grep -c 'var ErrCloneTargetNotEmpty' internal/style/errors.go` == 1 (sentinel added to the 02-01/02-02 file)
    - `grep -c 'func CloneStyles' internal/style/cloner.go` == 1
    - `grep -c 'var cloneParts' internal/style/cloner.go` == 1 (constant table present)
    - `grep -c 'word/styles.xml' internal/style/cloner.go` == 1
    - `grep -c 'word/numbering.xml' internal/style/cloner.go` == 1
    - `grep -c 'word/fontTable.xml' internal/style/cloner.go` == 1
    - `grep -c 'word/theme/theme1.xml' internal/style/cloner.go` == 1
    - `grep -c 'word/settings.xml' internal/style/cloner.go` == 1
    - `grep -c 'dst.MarkModified' internal/style/cloner.go` == 1 (byte pass-through, D-09)
    - `grep -c 'NextRID' internal/style/cloner.go` == 1 (rId allocation, OPC-06)
    - `grep -c 'dst.ContentTypes.Overrides' internal/style/cloner.go` == 1 (content-type wiring)
    - `grep -c '"/" + p.name\|"/"\s*+\s*p\.name\|"/\ "%.*p\.name' internal/style/cloner.go` >= 1 (leading-slash key — PATTERNS critical flag)
    - `grep -v '^//' internal/style/cloner.go | grep -c 'ErrCloneTargetNotEmpty'` >= 1 (D-08 precondition guard in code, not just comments)
    - `grep -v '^//' internal/style/cloner.go | grep -c 'xmlutil\|wml\.'` == 0 (no re-parse — D-09 enforced structurally)
    - `grep -c 'func Test' internal/style/cloner_test.go` >= 8 (one per behavior case)
    - `grep -c 'errors.Is(err, ErrCloneTargetNotEmpty)' internal/style/cloner_test.go` >= 1 (D-08 assertion)
    - `grep -c 'recover()' internal/style/cloner_test.go` >= 1 (no-panic guard on nil-package test)
    - `go test ./internal/style/... -count=1 -run Clone` passes (all cloner subtests green)
    - `go test ./internal/style/... -count=1` passes (no regression in 02-01 resolver + 02-02 theme/numbering tests — the errors.go sentinel addition is additive)
    - `go test ./internal/opc/... -count=1` passes (no opc regression — cloner only consumes opc, does not modify it)
    - `go vet ./internal/style/...` clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 2, depends_on [02-01, 02-02]): Build internal/style/corpus_test.go + expected.json schema files — real-template test corpus validation harness (STYLE-CLONE-02 + ROADMAP success criterion #4)</name>
  <files>
    internal/style/corpus_test.go,
    testdata/word/style-rich/heading-chain.expected.json,
    testdata/word/style-rich/theme-refs.expected.json,
    testdata/word/style-rich/multi-level-numbering.expected.json,
    testdata/style-engine/hostile/circular-basedon.expected.json,
    testdata/style-engine/hostile/dangling-basedon.expected.json,
    testdata/style-engine/hostile/missing-numid.expected.json
  </files>
  <read_first>
    - internal/style/cloner.go (Task 1 output — confirms CloneStyles signature, cloneParts table, ErrCloneTargetNotEmpty)
    - internal/style/resolver.go (02-01 output — confirms NewResolver, ResolveParagraph(*wml.CT_P) (*wml.CT_PPr, error), ResolveRun(*wml.CT_P, *wml.CT_R) (*wml.CT_RPr, error), Warnings() []string signatures; confirms Resolver operates over *opc.Package)
    - internal/style/theme.go (02-02 output — confirms themeCache.ResolveColor is wired into ResolveRun; the corpus validation exercises end-to-end theme color concretization over cloned packages)
    - internal/style/numbering.go (02-02 output — confirms numberingCache.ResolveLvl is wired into ResolveParagraph; the corpus validation exercises end-to-end numbering level pPr merge over cloned packages)
    - internal/style/cloner_test.go (Task 1 output — reuse buildSyntheticSourceZip/buildSyntheticTargetZip helpers; copy buildSyntheticZip pattern)
    - internal/opc/opc_test.go (buildSyntheticZip:47-73, HasFixtures t.Skip:86-95, errors.Is:240-242, no-panic guard:573-583)
    - internal/wml/styles.go (CT_Styles, CT_Style, CT_DocDefaults, CT_BasedOn — the structures the synthetic fixtures author; CT_Style.StyleID, BasedOn.Val, PPr, RPr fields)
    - internal/wml/numbering.go (CT_Numbering, CT_AbstractNum, CT_Num, CT_Lvl — the structures the synthetic numbering fixture authors; CT_Num.LvlOverride from 02-02 Task 1)
    - internal/wml/document.go (CT_P, CT_PPr, CT_R, CT_RPr — the structures the expected.json schema describes; field names for the equality assertion)
    - internal/wml/properties.go (CT_Spacing, CT_Ind, CT_RFonts, CT_Color, CT_NumPr — the composite-leaf fields the equality assertion compares field-by-field)
    - defaults/styles.xml (reference for a complete styles tree with Heading1/Heading2/Normal + docDefaults; mine for the synthetic heading-chain fixture's XML content)
    - defaults/theme1.xml (reference for a:clrScheme children: dk1/lt1 sysClr lastClr, accent1-6 srgbClr — mine for the synthetic theme-refs fixture)
    - .planning/phases/02-style-engine/02-RESEARCH.md §Fixture Design (lines 596-679 — expected.json schema spec, canonical Heading2 case lines 646-654, hostile dir layout lines 657-679, testdata layout summary lines 663-679), §Validation Architecture (lines 681-697+ — assertion strategies: field-by-field nil-check, Warnings() substring, clone correctness)
    - .planning/phases/02-style-engine/02-PATTERNS.md §"internal/style/*_test.go" (lines 387-448 — synthetic fixture builder, errors.Is, fixture-presence skip, subtest structure, no-panic guard), §"testdata/word/style-rich/ + testdata/style-engine/hostile/" (lines 452-459 — expected.json is a user-authored schema per Phase 1 D-02; this plan delivers the schema + synthetic equivalents; real .docx are user-authored via user_setup)
    - .planning/phases/02-style-engine/02-CONTEXT.md D-10 (hand-authored expected-values fixtures under testdata/word/style-rich/), D-11 (fixture shape: deep basedOn chain, themeColor refs, multi-level numbering; hostile fixtures covered by warning-path tests from D-05/D-07), D-05 (circular basedOn: warning + last-good), D-07 (missing ref: warning + default), D-08 (clone is fresh-empty-target; corpus validation asserts ErrCloneTargetNotEmpty path)
    - .planning/phases/02-style-engine/02-01-PLAN.md (confirm the resolver's Warnings() surface and the synthetic fixture pattern this task mirrors)
    - .planning/phases/02-style-engine/02-02-PLAN.md (confirm the theme/numbering post-processing wiring this task exercises end-to-end over cloned packages)
  </read_first>
  <behavior>
    - Heading2 chain equality (ROADMAP success criterion #4): a synthetic heading-chain.docx (built in-test via zip.Writer) with styles Normal→Heading1→Heading2 and a paragraph pStyle="Heading2" → CloneStyles into a fresh target → NewResolver(clonedTarget) → ResolveParagraph(heading2Para) → assert the returned CT_PPr.Spacing.Before/After, KeepNext, OutlineLvl match heading-chain.expected.json field-by-field; ResolveRun on a run in the same paragraph → assert CT_RPr.Sz, B, Color (concretized from themeColor via 02-02), RFonts match the expected.json field-by-field.
    - Theme color refs: a synthetic theme-refs.docx with styles referencing themeColor="accent1" and themeColor="dark1" (sysClr lastClr) in docDefaults + a paragraph style → CloneStyles → ResolveRun → assert result.Color.Val equals the accent1/dark1 hex from the cloned theme1.xml (D-06 concretization end-to-end over a cloned package); assert result.Color.ThemeColor is nil (cleared by 02-02).
    - Multi-level numbering: a synthetic multi-level-numbering.docx with numbering.xml defining abstractNum 0 (3 levels: decimal %1., alpha %2., bullet •) + num 1 → abstractNumId=0, and a paragraph with numPr numId=1 ilvl=0/1/2 → CloneStyles → ResolveParagraph at each ilvl → assert result.Ind.Left matches the level's pPr ind (merged via 02-02 numbering.ResolveLvl) and result.NumPr preserved.
    - Circular basedOn (D-05): a synthetic circular-basedon.docx with CycleA.basedOn=CycleB, CycleB.basedOn=CycleA → CloneStyles → NewResolver → ResolveParagraph(pStyle=CycleA) → assert: no panic (recover guard), Warnings() contains a string with substring "circular basedOn chain", returned pPr is non-nil and reflects props accumulated before the cycle break.
    - Dangling basedOn (D-07): a synthetic dangling-basedon.docx with Ghost.basedOn=Missing (Missing not in styles.xml or latentStyles) → CloneStyles → ResolveParagraph(pStyle=Ghost) → assert: Warnings() contains "not found in styles.xml or latentStyles"; returned pPr reflects Ghost's own pPr merged over docDefaults.
    - Missing numId (D-07): a synthetic missing-numid.docx with a paragraph numPr numId=999 (999 not in numbering.xml) → CloneStyles → ResolveParagraph → assert: Warnings() contains "numId" and "not found"; NumPr preserved on result; no level pPr merge (Ind unchanged from chain).
    - Clone correctness via corpus: for each style-rich fixture, assert that after CloneStyles + opc.Save(clonedTarget) → re-open → the 5 parts are byte-identical to the source (D-09 + OPC-04, end-to-end over the corpus fixtures, not just the Task 1 synthetic).
    - Clone target non-empty via corpus: assert CloneStyles returns ErrCloneTargetNotEmpty when the target already has styles.xml (D-08 end-to-end over a corpus-derived target).
    - Expected.json schema loader: the harness loads `testdata/word/style-rich/*.expected.json` and `testdata/style-engine/hostile/*.expected.json` via `encoding/json` into a struct matching the 02-RESEARCH §Fixture Design schema (template, description, cases[] with id/locator/pStyle/expected{pPr,rPr}/warnings[]).
    - Field-by-field nil-check equality: the assertion helper compares the resolved CT_PPr/CT_RPr against the expected.json `expected` block — for each pointer field, `nil` in JSON means the pointer must be nil in the resolved struct; a value in JSON means the pointer must be non-nil with the matching value. Use `reflect.DeepEqual` after nil-normalization (RESEARCH Validation Architecture line 697 — "nil ≠ &CT_OnOff{}" distinction).
    - Real .docx fixtures absent: the t.Skip-guarded hooks (one per fixture) check `testdata/word/style-rich/<name>.docx` presence via `os.Stat`; if absent, the test skips with a message naming the user_setup artifact the user must author. The synthetic in-memory equivalents run unconditionally in CI and cover the same assertion paths.
    - Named styles applicable by name (STYLE-CLONE-02): after CloneStyles, the cloned target's styles.xml is resolvable by styleId via the 02-01 resolver — a paragraph with pStyle="Heading2" in the cloned target resolves the same effective props as the source. This is the STYLE-CLONE-02 acceptance test (named styles from template applicable to new content by name).
  </behavior>
  <action>
    Create the 6 `expected.json` schema files FIRST (the harness loads them; tests fail RED if the schema is wrong). Each file follows the 02-RESEARCH §Fixture Design schema (lines 599-639):

    `testdata/word/style-rich/heading-chain.expected.json` — describes the canonical Heading2 chain (ROADMAP success criterion #4). Fields: `template` ("heading-chain.docx"), `description`, `cases` array with one case `id:"h2-para-1"`, `locator:"paragraph[1]"`, `pStyle:"Heading2"`, `expected.pPr` (spacing before=240 after=120, outlineLvl=1, keepNext=true, ind=null, jc=null, numPr=null) and `expected.rPr` (rFonts asciiTheme="majorHAnsi" ascii=null, sz val=28, color val=<accent1 hex from theme1.xml>, b=true, i=null), `warnings:[]`. Use the exact Heading2 case values from 02-RESEARCH lines 646-654. The `null` vs `0`/`""` distinction matters per RESEARCH Validation Architecture — `null` means nil pointer (absent), `0`/`""` means present-with-zero-value.

    `testdata/word/style-rich/theme-refs.expected.json` — describes themeColor refs in styles + docDefaults. Cases: (a) a run whose effective color has themeColor="accent1" → expected rPr.color.val=<accent1 hex> with themeColor=null (cleared by 02-02); (b) a run whose effective color has themeColor="dark1" → expected rPr.color.val=<dk1 lastClr hex> with themeColor=null (sysClr lastClr path); (c) a run whose effective color has themeColor="hyperlink" → expected rPr.color.val=<hlink hex> with themeColor=null (Pitfall 3 enum→element map end-to-end). Each case's `warnings` is empty (successful resolution).

    `testdata/word/style-rich/multi-level-numbering.expected.json` — describes multi-level numbering. Cases: (a) ilvl=0 → expected pPr.ind.left=720, numPr preserved; (b) ilvl=1 → expected pPr.ind.left=1440, numPr preserved; (c) ilvl=2 → expected pPr.ind.left=2160, numPr preserved. Each case's `warnings` is empty.

    `testdata/style-engine/hostile/circular-basedon.expected.json` — describes the D-05 cycle. Fields: `template` ("circular-basedon.docx"), `description`, `cases` array with one case `id:"cycle-para"`, `locator:"paragraph[0]"`, `pStyle:"CycleA"`, `expected.pPr` (minimal — cycle breaks before merge completes; assert non-nil but do not assert specific field values, only assert that the result reflects props accumulated before the cycle node was re-entered), `expected.rPr` (empty), `warnings:["style: circular basedOn chain at \"CycleA\""]` (exact substring per 02-01 resolver warning text).

    `testdata/style-engine/hostile/dangling-basedon.expected.json` — describes the D-07 dangling ref. Cases: one case `id:"dangling-para"`, `pStyle:"Ghost"`, `expected.pPr` (Ghost's own pPr merged over docDefaults — assert specific fields the synthetic fixture sets), `warnings:["style: \"Missing\" not found in styles.xml or latentStyles"]`.

    `testdata/style-engine/hostile/missing-numid.expected.json` — describes the D-07 missing numId. Cases: one case `id:"missing-numid-para"`, `pStyle:"Normal"`, `expected.pPr` (numPr preserved with numId=999, no level ind merge — ind.left=null or unchanged from chain), `warnings:["numbering: numId 999 not found in numbering.xml"]`.

    All 6 JSON files: indent with 2 spaces; the schema is a stable contract — once committed, the harness loads them at test time via `encoding/json.Unmarshal`. Author them by hand from reading the synthetic fixture's XML (the executor writes both the synthetic fixture XML AND the expected.json, so the values are self-consistent).

    Create `internal/style/corpus_test.go` with package `style` (white-box — same package, can access unexported `cloneParts`, `Resolver` fields if needed for assertion). Imports: `encoding/json`, `encoding/xml` (for fixture XML construction), `bytes`, `errors`, `fmt`, `os`, `path/filepath`, `reflect`, `strings`, `testing`, `archive/zip`, `github.com/fabiomarini/wordingo/internal/opc`, `github.com/fabiomarini/wordingo/internal/wml`.

    Define the expected.json schema Go structs (mirror 02-RESEARCH lines 599-639):
    - `type expectedFixture struct { Template string `json:"template"`; Description string `json:"description"`; Cases []expectedCase `json:"cases"` }`
    - `type expectedCase struct { ID string `json:"id"`; Locator string `json:"locator"`; PStyle string `json:"pStyle"`; Expected expectedProps `json:"expected"`; Warnings []string `json:"warnings"` }`
    - `type expectedProps struct { PPr map[string]any `json:"pPr"`; RPr map[string]any `json:"rPr"` }` — use `map[string]any` for flexible field-by-field comparison (the JSON schema is hand-authored and may evolve; a map is more resilient than a rigid struct). The assertion helper walks the map and compares each key against the resolved CT_PPr/CT_RPr field via reflection or a switch.

    Define the synthetic fixture builders (one per fixture, mirroring 02-01's `buildStylesXML` + `buildPkgWithStyles` pattern):
    - `buildHeadingChainSourceZip(t *testing.T) []byte` — a ZIP with styles.xml (Normal rFonts minorHAnsi sz 22; Heading1 basedOn Normal sz 32 b=true keepNext spacing before=480 after=240 color themeColor="accent1" outlineLvl=0; Heading2 basedOn Heading1 sz 28 spacing before=240 after=120 outlineLvl=1) + theme1.xml (accent1 srgbClr val="156082", dk1 sysClr lastClr="000000") + numbering.xml (none — not needed for this fixture) + fontTable.xml + settings.xml (minimal) + document.xml with one paragraph pStyle="Heading2" containing one run with no direct rPr. Use defaults/styles.xml + defaults/theme1.xml as content source where the values match (read via os.ReadFile relative to repo root using `runtime.Caller` for path resolution, OR embed minimal XML string literals — the latter is hermetic and preferred).
    - `buildThemeRefsSourceZip(t *testing.T) []byte` — styles.xml with docDefaults rPr color themeColor="dark1" + a paragraph style rPr color themeColor="accent1" + a character style rPr color themeColor="hyperlink"; theme1.xml with all 12 a:clrScheme children (dk1/lt1 sysClr, others srgbClr); document.xml with paragraphs/runs exercising each themeColor.
    - `buildMultiLevelNumberingSourceZip(t *testing.T) []byte` — numbering.xml with abstractNum 0 (3 levels: decimal %1. ind left=720; alpha %2. ind left=1440; bullet • ind left=2160) + num 1 abstractNumId=0; styles.xml with Normal; document.xml with 3 paragraphs each pStyle="Normal" with numPr numId=1 ilvl=0/1/2.
    - `buildCircularBasedOnSourceZip(t *testing.T) []byte` — styles.xml with CycleA.basedOn=CycleB, CycleB.basedOn=CycleA, CycleA spacing before=100; document.xml with paragraph pStyle="CycleA".
    - `buildDanglingBasedOnSourceZip(t *testing.T) []byte` — styles.xml with Ghost.basedOn=Missing (Missing not defined), Ghost spacing before=200; document.xml with paragraph pStyle="Ghost".
    - `buildMissingNumIdSourceZip(t *testing.T) []byte` — styles.xml with Normal; numbering.xml with abstractNum 0 + num 1 (no num 999); document.xml with paragraph pStyle="Normal" numPr numId=999 ilvl=0.
    - `buildFreshEmptyTargetZip(t *testing.T) []byte` — reuse Task 1's buildSyntheticTargetZip pattern.

    Define the harness loader:
    - `func loadExpectedFixture(t *testing.T, path string) *expectedFixture` — `os.ReadFile` + `json.Unmarshal` + `t.Fatalf` on error. Path is relative to repo root (use `runtime.Caller(0)` to resolve the test file's directory, then walk up to find `testdata/`).
    - `func hasRealDocx(path string) bool` — `os.Stat` the .docx path; return true if present. Used by the t.Skip guards.

    Define the assertion helpers:
    - `func assertEffectivePPr(t *testing.T, got *wml.CT_PPr, expected map[string]any)` — walk the expected map; for each key (e.g. "spacing", "keepNext", "outlineLvl", "numPr", "ind", "jc"), assert the corresponding field on `got` matches. Use a switch on the key: "spacing" → assert `got.Spacing` non-nil (or nil if expected is null) and walk the spacing sub-map (before/after/line/lineRule) comparing `got.Spacing.Before` etc.; "keepNext" → assert `got.KeepNext` non-nil and `*got.KeepNext.Val` matches; "outlineLvl" → assert `got.OutlineLvl` non-nil and matches; "numPr" → if null, assert `got.NumPr == nil`; if non-null, assert `got.NumPr` non-nil and walk numId/ilvl. Use `t.Errorf` per field (not `t.Fatalf`) so all field mismatches are reported in one run.
    - `func assertEffectiveRPr(t *testing.T, got *wml.CT_RPr, expected map[string]any)` — symmetric: "sz", "b", "i", "color", "rFonts". For "color", walk val/themeColor (assert themeColor is null after 02-02 concretization; assert val matches the expected hex).
    - `func assertWarnings(t *testing.T, got []string, expected []string)` — for each expected substring, assert some element of `got` contains it (substring match, not exact — the resolver may append additional context). Use `strings.Contains` loop.
    - `func normalizeNilPPr(p *wml.CT_PPr) *wml.CT_PPr` — returns `&wml.CT_PPr{}` if p is nil so `reflect.DeepEqual` compares struct values not nil-vs-zero (RESEARCH Validation Architecture line 697).

    Define the test cases (one `func Test...` per fixture, using `t.Run` subtests for multi-case fixtures like theme-refs and multi-level-numbering):
    - `TestCorpus_Heading2Chain` (ROADMAP success criterion #4): buildHeadingChainSourceZip + buildFreshEmptyTargetZip → opc.Open both → CloneStyles → NewResolver(clonedTarget) → ResolveParagraph(heading2Para) → loadExpectedFixture("heading-chain.expected.json") → assertEffectivePPr + ResolveRun → assertEffectiveRPr + assertWarnings (empty). Also assert clone correctness: opc.Save(clonedTarget) → re-open → 5 parts byte-identical to source.
    - `TestCorpus_ThemeRefs`: buildThemeRefsSourceZip + clone + resolve → 3 subtests (accent1, dark1, hyperlink) asserting concretized color val + themeColor cleared. Also assert theme1.xml byte-identical after clone (D-06 read-only end-to-end).
    - `TestCorpus_MultiLevelNumbering`: buildMultiLevelNumberingSourceZip + clone + resolve → 3 subtests (ilvl 0/1/2) asserting merged ind.left + numPr preserved. Also assert numbering.xml byte-identical after clone.
    - `TestCorpus_CircularBasedOn` (D-05): buildCircularBasedOnSourceZip + clone + resolve → assert no-panic (recover guard) + warning substring + non-nil pPr. Load circular-basedon.expected.json for the warning assertion.
    - `TestCorpus_DanglingBasedOn` (D-07): buildDanglingBasedOnSourceZip + clone + resolve → assert warning substring + pPr reflects Ghost merged over docDefaults. Load dangling-basedon.expected.json.
    - `TestCorpus_MissingNumId` (D-07): buildMissingNumIdSourceZip + clone + resolve → assert warning substring + numPr preserved + no ind merge. Load missing-numid.expected.json.
    - `TestCorpus_CloneTargetNotEmpty`: buildHeadingChainSourceZip + buildSyntheticTargetWithStylesZip (from Task 1) → CloneStyles → assert ErrCloneTargetNotEmpty (D-08 end-to-end over a corpus-derived source).
    - `TestCorpus_NamedStylesApplicableByName` (STYLE-CLONE-02): buildHeadingChainSourceZip + clone into fresh target → NewResolver(clonedTarget) → ResolveParagraph(p with pStyle="Heading2") → assert non-nil result with Heading2's effective props. This proves the cloned template's named styles are applicable by name in the new document (STYLE-CLONE-02). The assertion is the same as TestCorpus_Heading2Chain's pPr assertion — this test exists as a separate, named acceptance test for the requirement ID.
    - `TestCorpus_RealFixtures` (t.Skip-guarded): iterate `testdata/word/style-rich/*.docx` + `testdata/style-engine/hostile/*.docx`; for each present .docx, open it, clone into a fresh target, resolve, assert against the corresponding expected.json. Skips entirely if no real .docx are present (the synthetic tests above cover the same paths in CI). This is the hook for user-authored fixtures per user_setup.

    Add no-panic recover guards to the hostile tests (circular, dangling, missing-numid) mirroring `opc_test.go:573-583` — the resolver must not stack-overflow on circular basedOn (02-01 cycle detection via visited-set, but the guard is defense-in-depth).

    Test command: `go test ./internal/style/... -count=1 -run Corpus -v`.

    Do NOT modify resolver.go, theme.go, numbering.go, or errors.go in this task — those are 02-01/02-02/Task 1 outputs. This task only adds corpus_test.go and the expected.json schema files. If the resolver/theme/numbering API surface needs adjustment, that is a gap to surface to the orchestrator, not a silent edit in this task.
  </action>
  <verify>
    <automated>cd /Users/fabiomarini/Development/go/wordingo && go build ./internal/style/... && go vet ./internal/style/... && go test ./internal/style/... -count=1 -run Corpus -v</automated>
  </verify>
  <done>
    - `test -f testdata/word/style-rich/heading-chain.expected.json` (schema file exists)
    - `test -f testdata/word/style-rich/theme-refs.expected.json`
    - `test -f testdata/word/style-rich/multi-level-numbering.expected.json`
    - `test -f testdata/style-engine/hostile/circular-basedon.expected.json`
    - `test -f testdata/style-engine/hostile/dangling-basedon.expected.json`
    - `test -f testdata/style-engine/hostile/missing-numid.expected.json`
    - `grep -c '"pStyle"' testdata/word/style-rich/heading-chain.expected.json` >= 1 (schema has pStyle field per case)
    - `grep -c '"warnings"' testdata/style-engine/hostile/circular-basedon.expected.json` >= 1 (hostile schema has warnings array)
    - `grep -c 'circular basedOn chain' testdata/style-engine/hostile/circular-basedon.expected.json` >= 1 (warning substring asserted)
    - `grep -c 'func TestCorpus' internal/style/corpus_test.go` >= 8 (one per fixture + clone-target-not-empty + named-styles + real-fixtures-skip)
    - `grep -c 'func loadExpectedFixture' internal/style/corpus_test.go` == 1 (harness loader present)
    - `grep -c 'func assertEffectivePPr' internal/style/corpus_test.go` == 1 (equality assertion helper)
    - `grep -c 'func assertEffectiveRPr' internal/style/corpus_test.go` == 1
    - `grep -c 'func assertWarnings' internal/style/corpus_test.go` == 1
    - `grep -c 'CloneStyles' internal/style/corpus_test.go` >= 8 (every corpus test exercises the cloner end-to-end)
    - `grep -c 'NewResolver' internal/style/corpus_test.go` >= 6 (every resolve test exercises the 02-01 resolver over a cloned package)
    - `grep -c 'recover()' internal/style/corpus_test.go` >= 1 (no-panic guard on hostile tests)
    - `grep -c 't.Skip' internal/style/corpus_test.go` >= 1 (real-fixture skip guard)
    - `go test ./internal/style/... -count=1 -run Corpus` passes (all corpus subtests green)
    - `go test ./internal/style/... -count=1` passes (no regression in Task 1 cloner + 02-01 resolver + 02-02 theme/numbering)
    - `go test ./...` passes (no package regressed)
    - `go vet ./internal/style/...` clean
    - No real `.docx` files created under `testdata/` (those are user-authored per user_setup; only `expected.json` schema files + synthetic in-memory builders are delivered)
  </done>
</task>

</tasks>

<artifacts_phase_produces>
## Artifacts this phase produces (across plans 02-01, 02-02, 02-03)

This plan (02-03) produces:

**Cloner (Task 1, Wave 1):**
- `internal/style/errors.go` — `ErrCloneTargetNotEmpty` sentinel added (extends the 02-01/02-02 file; the 4th and final style-package sentinel for v1: ErrStylesParseFailed, ErrThemeParseFailed, ErrNumberingParseFailed, ErrCloneTargetNotEmpty).
- `internal/style/cloner.go` — `CloneStyles(src, dst *opc.Package) error`; `cloneParts` constant table (5 entries: word/styles.xml, word/numbering.xml, word/fontTable.xml, word/theme/theme1.xml, word/settings.xml with their OOXML relationship type URIs and content-type MIMEs). Composes Phase 1's `opc.MarkModified` (byte pass-through, D-09), `opc.Relationships.NextRID` (rId allocation, OPC-06), `opc.ContentTypes.Overrides` (content-type wiring, leading-slash key per PATTERNS critical flag). No re-parse, no `internal/wml` or `internal/xmlutil` imports (D-09 enforced structurally).
- `internal/style/cloner_test.go` — synthetic-fixture test suite covering: fresh-empty-target success, ErrCloneTargetNotEmpty on non-empty target (atomic precondition scan), absent-source-part skip, relationship rId allocation + uniqueness, content-type Override registration with leading slash, opc.Save round-trip byte identity, no-re-parse structural assertion, idempotent-fails, nil-package defensive no-panic. Reuses `opc_test.go:47-73` `buildSyntheticZip` pattern (copied, not exported).

**Test corpus + validation harness (Task 2, Wave 2):**
- `internal/style/corpus_test.go` — synthetic style-rich + hostile fixture builders (heading-chain, theme-refs, multi-level-numbering, circular-basedon, dangling-basedon, missing-numid); `expectedFixture`/`expectedCase`/`expectedProps` schema structs; `loadExpectedFixture` harness loader; `assertEffectivePPr`/`assertEffectiveRPr`/`assertWarnings` field-by-field nil-check equality helpers; `normalizeNilPPr` nil-vs-zero normalization; t.Skip-guarded real-fixture hooks. Exercises the cloner (Task 1) + 02-01 resolver + 02-02 theme/numbering post-processors end-to-end over cloned packages — STYLE-CLONE-02 (named styles applicable by name) + ROADMAP success criterion #4 (Heading2 chain).
- `testdata/word/style-rich/heading-chain.expected.json` — canonical Heading2 chain expected-values (ROADMAP success criterion #4).
- `testdata/word/style-rich/theme-refs.expected.json` — themeColor refs (accent1, dark1 sysClr, hyperlink) expected-values.
- `testdata/word/style-rich/multi-level-numbering.expected.json` — 3-level numbering expected-values.
- `testdata/style-engine/hostile/circular-basedon.expected.json` — D-05 cycle expected-values + warnings.
- `testdata/style-engine/hostile/dangling-basedon.expected.json` — D-07 dangling ref expected-values + warnings.
- `testdata/style-engine/hostile/missing-numid.expected.json` — D-07 missing numId expected-values + warnings.

Plan 02-01 already produced (upstream — Task 2 depends on):
- `internal/opc/package.go` — `Part.IsModified() bool` accessor
- `internal/style/resolver.go` — `Resolver`, `NewResolver`, `ResolveParagraph`, `ResolveRun`, `Warnings`, chain walker, memo, cycle detection, merge/clone helpers
- `internal/style/errors.go` — `ErrStylesParseFailed` sentinel
- `internal/style/resolver_test.go` — 02-01 synthetic test suite

Plan 02-02 already produced (upstream — Task 2 depends on):
- `internal/wml/numbering.go` — `CT_Num.LvlOverride` field + `CT_LvlOverride` + `CT_StartOverride` types (Pitfall 4 fix)
- `internal/style/theme.go` — `themeEnumToElement` map, `themeCache`, `ResolveColor`, tint/shade math
- `internal/style/numbering.go` — `numberingCache`, `ResolveLvl`, lvlOverride handling
- `internal/style/errors.go` — `ErrThemeParseFailed` + `ErrNumberingParseFailed` sentinels
- `internal/style/theme_test.go` + `internal/style/numbering_test.go` — 02-02 synthetic test suites
- `internal/style/resolver.go` (modified) — theme + numbering post-processing wired into ResolveRun/ResolveParagraph

Phase 3 will consume (downstream):
- `internal/style/cloner.go` `CloneStyles` — Phase 3 `Document.FromTemplate()` (CREATE-03) calls it under the hood
- The `expected.json` schema — Phase 3 STYLE-ROUNDTRIP-01/02 validation may extend the corpus
</artifacts_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted source .docx → CloneStyles | A template .docx opened via `opc.Open` (which enforces OPC-07 safety limits: zip bomb, path traversal, max parts) crosses into the cloner's byte-copy path. Source parts are read via `Part.Open` (capped at `MaxPartBytes` = 128 MiB) and written into the target via `MarkModified`. The cloner never parses the source parts (D-09) — no XML decoder runs on clone, so malformed XML does not surface here (it surfaces at resolve time per D-07). |
| CloneStyles → target package | The 5 source parts' bytes + 5 new relationships + 5 content-type Overrides cross into the target package. All part names are compile-time constants (the `cloneParts` table) — no user-supplied names reach `MarkModified`, `ContentTypes.Overrides`, or `Relationship.Target`. Path traversal is structurally impossible in the cloner. |
| cloned target → 02-01/02-02 resolver | The cloned parts' bytes cross into the resolver's lazy parse path when `ResolveParagraph`/`ResolveRun` is called over the cloned target. This boundary is owned by 02-01 (styles.xml parse) and 02-02 (theme1.xml + numbering.xml parse) — their threat models (T-02-01 through T-02-11) cover it. This plan's corpus_test.go exercises that boundary end-to-end but does not add new threats there. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Tampering / Path Traversal | `CloneStyles` — writing parts outside the target package | high | mitigate | All 5 part names are compile-time string constants in the `cloneParts` table (word/styles.xml, word/numbering.xml, word/fontTable.xml, word/theme/theme1.xml, word/settings.xml). No user-supplied or source-derived names reach `dst.MarkModified`, `dst.ContentTypes.Overrides`, or `Relationship.Target`. The cloner does not read source relationship targets to decide what to clone — it clones a fixed, hardcoded set. Path traversal is structurally impossible (no `path.Join`, no user input in part names). Additionally, `opc.Open` already ran `validatePartName` on every source entry (package.go:352-380 rejects absolute paths, `..`, backslashes, drive letters, non-UTF-8), so even a hostile source .docx cannot introduce a malformed part name that the cloner would copy — the source parts are already validated. `opc.Save` re-validates the relationship graph (relationships.go:161-191) and content-type presence (package.go:280) before writing, so a corrupted target state is caught at save time. |
| T-02-13 | DoS / Resource Exhaustion | `CloneStyles` — reading source part bytes into memory | medium | mitigate | `Part.Open` enforces `MaxPartBytes` = 128 MiB per part via `capReader` (package.go:78-83 returns `ErrDecompressionLimit` past the cap). `opc.Open` enforces `MaxTotalBytes` = 512 MiB across all parts (package.go:161-164). The cloner reads one part at a time via `io.ReadAll` on the capped reader; peak memory is bounded by the largest single part (≤128 MiB), not the sum. The 5 cloned parts together are typically < 1 MiB in practice (styles.xml ~50 KiB, theme1.xml ~10 KiB, numbering.xml ~20 KiB, fontTable.xml ~5 KiB, settings.xml ~5 KiB). No unbounded allocation. |
| T-02-14 | Tampering | `CloneStyles` — partial clone on non-empty target (atomicity violation) | high | mitigate | D-08 precondition scan: the cloner iterates all 5 `cloneParts` entries and checks `dst.Parts[p.name]` existence BEFORE any byte copy. If any of the 5 parts already exists in the target, the cloner returns `ErrCloneTargetNotEmpty` immediately — no `MarkModified`, no relationship added, no content-type Override set. The target is left in its pre-call state. This is the same guard shape as `opc.Open`'s mandatory `[Content_Types].xml` check (package.go:175-178). Test `TestCloneStyles_ErrCloneTargetNotEmpty` asserts the target's pre-existing styles.xml data is unchanged after the error (atomicity regression guard). |
| T-02-15 | Information Disclosure / Tampering | `CloneStyles` — relationship rId collision in target | medium | mitigate | `Relationships.NextRID` (relationships.go:138-145) is a high-water-mark allocator: it scans existing rIds on first call (`rescan`:63-71) and issues the next integer. Reuse is impossible by construction (OPC-06). The cloner calls `NextRID` once per cloned part, so 5 fresh rIds are allocated. `opc.Save` validates unique ids and dangling targets before writing (relationships.go:161-191) — a collision would surface at save time, not silently corrupt the package. |
| T-02-16 | Tampering | `CloneStyles` — content-type Override key missing leading slash | medium | mitigate | The PATTERNS critical flag (02-PATTERNS line 129) documents that `ContentTypes.Overrides` keys are OPC-absolute with a leading `/`. The cloner sets `dst.ContentTypes.Overrides["/"+p.name] = p.ct` — the leading slash is explicit in the code. `ContentTypes.TypeFor` (contenttypes.go:60-75) reads `c.Overrides["/"+partName]` — without the slash, `TypeFor` would miss the override and `opc.Save` would fail with "part has no content type" (package.go:280). Test `TestCloneStyles_ContentTypeOverrides` asserts the leading slash is present in every key (regression guard). |
| T-02-17 | DoS | `CloneStyles` — idempotent re-clone on already-cloned target | low | accept | Calling `CloneStyles` twice on the same dst fails on the second call with `ErrCloneTargetNotEmpty` (the first call populated the 5 parts — D-08 fresh-empty-target policy). This is by design (D-08), not a vulnerability — the error is clear and the target is not corrupted. Test `TestCloneStyles_IdempotentFails` asserts this behavior. |
| T-02-18 | Tampering / DoS | `corpus_test.go` — synthetic fixture builders | low | accept | The synthetic fixture builders author XML string literals in-test. The XML is constructed by the test author (the executor), not from untrusted input. A malformed fixture would fail the test's own assertions (the resolver would warn or return wrong props). No production code path is affected — corpus_test.go is test-only. |
| T-02-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No package manager in this plan — stdlib + internal packages only (PROJECT.md zero-dep constraint). RESEARCH §Package Legitimacy Audit: "Not applicable". No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages to gate. |

ASVS L1 disposition: the single high-severity threat (T-02-12 path traversal) is mitigated structurally — all part names are compile-time constants, no user input reaches the write path. The atomicity threat (T-02-14, also high) is mitigated by the precondition-scan-before-copy pattern. Medium-severity threats (T-02-13 resource exhaustion, T-02-15 rId collision, T-02-16 content-type key) are mitigated by Phase 1 opc layer enforcement + explicit code patterns. Low-severity items documented with explicit accept rationale (T-02-17 by-design, T-02-18 test-only). Block on high: T-02-12 and T-02-14 are both mitigated; no high-severity threat remains unmitigated.
</threat_model>

<verification>
## Plan-level verification (run after all tasks complete)

1. `go build ./...` — entire project compiles (cloner + corpus harness + 02-01 resolver + 02-02 theme/numbering + downstream still builds).
2. `go vet ./...` — clean (including cloner.go, corpus_test.go, and the 6 expected.json files are valid JSON).
3. `go test ./internal/style/... -count=1 -v` — all style tests green (Task 1 cloner_test + Task 2 corpus_test + 02-01 resolver_test + 02-02 theme_test + numbering_test).
4. `go test ./internal/wml/... -count=1` — 02-02's CT_Num struct extension still backward-compatible (no regression from this plan — this plan does not touch wml).
5. `go test ./internal/opc/... -count=1` — Phase 1 opc layer unregressed (this plan only consumes opc, does not modify it).
6. `go test ./...` — no package regressed across the whole project.
7. Manual D-09 check: `grep -v '^//' internal/style/cloner.go | grep -c 'xmlutil\|wml\.'` == 0 — confirms the cloner never parses (byte pass-through only). The corpus_test.go MAY import wml/xmlutil (it constructs fixtures and asserts on CT_PPr/CT_RPr) — the gate is scoped to cloner.go only (region-scoped negative gate, per planner-antipatterns.md "Region-Scoped Negative Gates" — a sibling file needs the construct elsewhere).
8. Manual D-08 check: `grep -v '^//' internal/style/cloner.go | grep -c 'ErrCloneTargetNotEmpty'` >= 1 — confirms the fresh-empty-target precondition is in code, not just comments.
9. Manual PATTERNS critical flag check: `grep -c '"/" + p.name\|"/"\s*+\s*p\.name' internal/style/cloner.go` >= 1 — confirms the content-type Override key has a leading slash.
10. Manual D-13 boundary check: `grep -v '^//' internal/style/cloner.go | grep -c 'ResolveLvl\|mergePPr\|numberingCache'` == 0 — confirms the cloner does not cross into 02-02 numbering resolution territory (cloner is byte-only; numbering resolve happens in the resolver).
11. Manual STYLE-CLONE-02 acceptance: `grep -c 'TestCorpus_NamedStylesApplicableByName' internal/style/corpus_test.go` == 1 — confirms the named-styles-by-name acceptance test exists.
12. Manual ROADMAP success criterion #4 check: `grep -c 'TestCorpus_Heading2Chain' internal/style/corpus_test.go` == 1 AND `test -f testdata/word/style-rich/heading-chain.expected.json` — confirms the canonical Heading2 chain test + expected-values fixture exist.
13. Manual real-fixture skip check: `grep -c 't.Skip' internal/style/corpus_test.go` >= 1 — confirms the real-fixture hooks skip gracefully when user-authored .docx are absent (CI runs on synthetic equivalents).
14. Decision coverage: every D-NN cited in an `<action>` body has a corresponding test: D-08 → TestCloneStyles_ErrCloneTargetNotEmpty + TestCorpus_CloneTargetNotEmpty; D-09 → TestCloneStyles_FreshEmptyTarget (byte pass-through) + TestCloneStyles_SaveRoundTrip (byte identity) + TestCloneStyles_NoReParse (structural); D-05 → TestCorpus_CircularBasedOn; D-07 → TestCorpus_DanglingBasedOn + TestCorpus_MissingNumId; D-10/D-11 → the 6 expected.json files + TestCorpus_* loading them; STYLE-CLONE-01 → TestCloneStyles_* (all cloner tests); STYLE-CLONE-02 → TestCorpus_NamedStylesApplicableByName.
15. Threat model coverage: T-02-12 (path traversal) → mitigated structurally (compile-time constants) + TestCloneStyles_NoReParse (no user input in parse path); T-02-14 (atomicity) → TestCloneStyles_ErrCloneTargetNotEmpty (asserts no partial clone); T-02-16 (leading slash) → TestCloneStyles_ContentTypeOverrides.
</verification>

<success_criteria>
## Measurable completion

- [ ] `internal/style/errors.go` declares `ErrCloneTargetNotEmpty` (extends 02-01/02-02 sentinel file; 4th and final v1 sentinel)
- [ ] `internal/style/cloner.go` defines `CloneStyles(src, dst *opc.Package) error` and the `cloneParts` constant table (5 entries with canonical OOXML URIs + MIMEs)
- [ ] CloneStyles byte-copies all 5 source parts via `opc.MarkModified` — no re-parse (D-09 enforced structurally: no `xmlutil`/`wml` imports in cloner.go)
- [ ] CloneStyles allocates fresh rIds via `Relationships.NextRID` and adds Relationships with canonical type URIs (OPC-06)
- [ ] CloneStyles registers content-type Overrides with leading-slash keys (PATTERNS critical flag)
- [ ] CloneStyles returns `ErrCloneTargetNotEmpty` on non-empty target — atomic precondition scan before any byte copy (D-08)
- [ ] CloneStyles skips absent source parts silently (no error, no warning)
- [ ] Cloned parts are byte-pass-through-identical after `opc.Save` round-trip (D-09 + OPC-04)
- [ ] `internal/style/cloner_test.go` covers: fresh-empty-target, ErrCloneTargetNotEmpty, absent-source-skip, relationship allocation, content-type overrides, save round-trip, no-re-parse, idempotent-fails, nil-defensive
- [ ] `internal/style/corpus_test.go` defines synthetic style-rich + hostile fixture builders, expected.json schema structs, harness loader, field-by-field nil-check equality helpers
- [ ] 6 `expected.json` schema files exist under `testdata/word/style-rich/` (3) and `testdata/style-engine/hostile/` (3) with the documented schema
- [ ] Heading2 chain (Heading2→Heading1→Normal) resolves effective CT_PPr/CT_RPr matching `heading-chain.expected.json` field-by-field (ROADMAP success criterion #4)
- [ ] Theme color refs (accent1, dark1 sysClr, hyperlink) concretize to concrete hex over cloned packages (D-06 end-to-end via 02-02)
- [ ] Multi-level numbering (3 levels) merges level pPr ind into effective pPr over cloned packages (D-13 end-to-end via 02-02)
- [ ] Circular basedOn (D-05), dangling basedOn (D-07), missing numId (D-07) surface via Warnings() containing expected substrings + no panic
- [ ] Named styles from cloned template applicable by name via 02-01 resolver (STYLE-CLONE-02 acceptance test)
- [ ] Real .docx fixtures absent → t.Skip with clear message; synthetic equivalents cover the same paths in CI (Phase 1 D-02 pattern)
- [ ] No real `.docx` files created under `testdata/` (user-authored per user_setup; only `expected.json` schema + synthetic in-memory builders delivered)
- [ ] All 02-01 resolver tests + 02-02 theme/numbering tests + Task 1 cloner tests pass (no regression)
- [ ] `go test ./internal/style/... -count=1` green; `go test ./...` green
- [ ] `<threat_model>` present with all threats dispositioned (mitigate/accept) and severity-assigned; T-02-12 and T-02-14 (high) both mitigated
</success_criteria>

<output>
Create `.planning/phases/02-style-engine/02-03-SUMMARY.md` when done. Record:
- Files created: `internal/style/cloner.go`, `internal/style/cloner_test.go`, `internal/style/corpus_test.go`, 6 `expected.json` schema files under `testdata/word/style-rich/` + `testdata/style-engine/hostile/`.
- Files modified: `internal/style/errors.go` (added `ErrCloneTargetNotEmpty` sentinel — 4th and final v1 style-package sentinel).
- The cloner API surface: `CloneStyles(src, dst *opc.Package) error`, `ErrCloneTargetNotEmpty`, `cloneParts` constant table (5 entries).
- The corpus harness API surface: `expectedFixture`/`expectedCase`/`expectedProps` schema structs, `loadExpectedFixture`, `assertEffectivePPr`/`assertEffectiveRPr`/`assertWarnings`/`normalizeNilPPr` helpers, 6 synthetic fixture builders (heading-chain, theme-refs, multi-level-numbering, circular-basedon, dangling-basedon, missing-numid).
- The expected.json schema: fields (template, description, cases[] with id/locator/pStyle/expected{pPr,rPr}/warnings[]), null-vs-zero semantics, hostile-schema warnings array.
- The wave structure: Task 1 Wave 1 (cloner, no deps), Task 2 Wave 2 (corpus validation, depends_on 02-01 + 02-02).
- The user_setup artifacts: 6 real .docx files user-authored in Word (heading-chain, theme-refs, multi-level-numbering, circular-basedon, dangling-basedon, missing-numid); the t.Skip-guarded TestCorpus_RealFixtures hook consumes them when present.
- The phase_boundary with Phase 3 (no FromTemplate/Document; cloner is a primitive operating on *opc.Package; Phase 3 Document.FromTemplate will call CloneStyles under the hood).
- The threat model: T-02-12 path traversal mitigated structurally (compile-time constants), T-02-14 atomicity mitigated (precondition scan before copy), T-02-16 leading-slash content-type key mitigated (explicit `"/" + p.name`).
- Any deviations from the plan (e.g., if the synthetic fixture XML needed adjustment to match a real Word quirk, if the expected.json schema needed an extra field for a CT_PPr field the assertion helper didn't cover, if the real-fixture t.Skip guard needed a different path resolution strategy).
</output>
