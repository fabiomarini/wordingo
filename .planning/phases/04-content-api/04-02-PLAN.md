---
phase: 04-content-api
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - paragraph.go
  - run.go
  - wordingo.go
  - style_test.go
autonomous: true
requirements:
  - API-03
  - QUAL-01
  - QUAL-03
user_setup: []
must_haves:
  truths:
    - Users call p.SetStyle("Title") to assign a paragraph style by name — sets pStyle reference
    - Users call r.SetStyle("Emphasis") to assign a run style by name — sets rStyle reference
    - Unknown style names produce a warning at Save time, not a panic
    - Resolver stays internal to Document — no public StyleResolver() accessor
    - Style parts are never MarkModified by content mutation code (D-06, STYLE-ROUNDTRIP-01)
    - X() escape hatch exists on Run for direct CT_R access (QUAL-03)
    - Open from io.ReaderAt, save to io.Writer works on content-rich docs (QUAL-01)
  artifacts:
    - Paragraph.SetStyle(name) with pStyle reference
    - Run.SetStyle(name) with rStyle reference
    - Style-name validation with Warnings() integration (deferred check at Save time)
    - Document.docWarnings() or equivalent for Save-time style validation flush
  key_links:
    - SetStyle on Paragraph sets ct.PPr.PStyle.Val = &name — never calls ResolveParagraph
    - SetStyle on Run sets ct.RPr.RStyle.Val = &name — never calls ResolveRun
    - D-06: MarkModified never called on style parts — only body part
    - D-07: Resolver stays internal — paragraph.go can access d.doc for resolver if needed internally
---

<objective>
Add named style application (API-03) and complete public API polish (QUAL-01, QUAL-03).

**Purpose:** Users apply paragraph and character styles by name (e.g., "Title", "Heading1", "Emphasis"). The library sets pStyle/rStyle XML references only — Word resolves at render time (D-05). Invalid style names produce deferred warnings. Public API surface is complete: Run has X() escape hatch (QUAL-03), I/O patterns work with content-rich documents (QUAL-01).

**Output:** Style application on Paragraph and Run, style-name validation with warning accumulation, Run.X() escape hatch (already on Run from 04-01 — verify), tests for style application, warning verification, and QUAL-01/QUAL-03 assertions.
</objective>

<execution_context>
@/Users/fabiomarini/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/fabiomarini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-api/04-CONTEXT.md
@.planning/phases/04-content-api/04-RESEARCH.md
@.planning/phases/04-content-api/04-PATTERNS.md
@.planning/phases/04-content-api/04-01-PLAN.md
@wordingo.go
@paragraph.go
@run.go
@template.go
@internal/wml/document.go
@internal/wml/properties.go
@internal/style/resolver.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade SetStyle with warning-based style validation and defer check to Save time</name>
  <files>
    paragraph.go
    run.go
    wordingo.go
    internal/style/resolver.go (read-only reference)
  </files>
  <behavior>
    - Paragraph.SetStyle("Title") sets ct.PPr.PStyle.Val = "Title", sets dirty, returns *Paragraph
    - Paragraph.SetStyle("") sets empty pStyle — valid (clears style). No warning.
    - Paragraph.SetStyle with valid-looking name (non-empty, no XML-illegal chars) — no warning at set time
    - Run.SetStyle("Emphasis") sets ct.RPr.RStyle.Val = "Emphasis", sets dirty, returns *Run
    - Run.SetStyle("") clears rStyle — valid. No warning.
    - Style name validation at Save time: serializeBody checks all paragraph/rStyle references against available style names
    - Unknown style name → warning appended to d.warnings
    - Style name checking uses Document's internal access to resolver (D-07 — resolver stays internal)
    - No pre-population of resolved properties (D-05)
    - D-06: MarkModified never called on style parts
  </behavior>
  <action>
    **Phase 4 CONTEXT.md D-05 through D-08 are authoritative. Per D-05: SetStyle sets only the style reference (pStyle/rStyle). No pre-population of resolved properties.**

    **paragraph.go** — upgrade `SetStyle` (created in 04-01 Task 2):
    1. Ensure `SetStyle(name string) *Paragraph` sets `ct.PPr.PStyle` via `&wml.CT_PStyle{Val: &name}`.
    2. Empty string name: set PStyle to nil (clears style). Check: if name == "" { p.ct.PPr.PStyle = nil; return p }.
    3. Per D-07: resolver stays internal. SetStyle does NOT call ResolveParagraph.
    4. Per D-12: validation deferred. No immediate warning for unknown style name. Warning happens at Save time (Task 1 step below: serializeBody check).
    5. Ensure nil-receiver guard panics with "wordingo: SetStyle called on nil Paragraph".

    **run.go** — upgrade `SetStyle` (created in 04-01 Task 2):
    1. Ensure `SetStyle(name string) *Run` sets `ct.RPr.RStyle` via `&wml.CT_RStyle{Val: &name}`.
    2. Empty string name: set RStyle to nil (clears style).
    3. Per D-07: no ResolveRun call.
    4. Per D-12: no immediate warning. Deferred to Save time.
    5. Ensure nil-receiver guard.

    **wordingo.go** — add Save-time style name validation per D-12 (full style catalog check):
    1. Add `checkStyleNames()` method on Document:
       - Read available style IDs from `d.pkg.Parts["word/styles.xml"]` — parse CT_Styles to extract each style's styleId attribute
       - Build a `map[string]bool` of known style IDs for O(1) lookup
       - Iterate `d.doc.Body.P` paragraphs:
         * For each Paragraph's PPr.PStyle: if non-nil and non-empty Val, check against known style IDs
         * For each run (CT_RPr.RStyle in each CT_R): if non-nil and non-empty Val, check against known style IDs
       - Unknown style ID → emit warning via `d.warn("unknown style %q referenced by paragraph/run")`
       - Also check for XML-illegal characters in style names (null bytes, control chars except tab/newline) — warn separately for each invalid name
       - Empty style name: valid (clears style reference per D-05) — no warning
    2. Call `checkStyleNames()` inside `serializeBody()` before CT_Document encoding, so warnings accumulate before Save completes.
    3. Per D-07: No public accessor for resolver. `checkStyleNames` reads the styles part directly via `d.pkg.Parts["word/styles.xml"]` — does NOT call internal/style/resolver. Resolver stays internal.
    4. Per D-06: Ensure serializeBody only calls MarkModified("word/document.xml", ...). Never MarkModified on style parts. Already guaranteed by architecture — only body part is modified.
  </action>
  <verify>
    <automated>go test ./... -run "TestSetStyle|TestStyleValidation" -count=1 -v 2>&1 | grep -E "PASS|FAIL|Error"</automated>
  </verify>
  <done>
    - Paragraph.SetStyle("Title") sets PStyle reference
    - Run.SetStyle("Emphasis") sets RStyle reference
    - Empty string clears style reference
    - XML-illegal characters in style name produce warning
    - Unknown style name referenced in body → warning via Warnings() at Save time (full D-12)
    - Style IDs read from styles.xml at Save time for validation
    - No pre-population of resolved properties (D-05)
    - Resolver stays internal (D-07)
    - serializeBody does not touch style parts
    - All existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: QUAL-01/QUAL-03 verification + comprehensive style + API surface tests</name>
  <files>
    style_test.go
  </files>
  <action>
    Create `style_test.go` with comprehensive tests for style application and quality requirements.

    **API-03 tests (style application):**
    1. `TestSetStyleParagraph`: Create doc → AddParagraph → SetStyle("Title") → Save → verify XML has w:pStyle w:val="Title".
    2. `TestSetStyleRun`: Create doc → AddParagraph → AddRun → SetStyle("Emphasis") → verify CT_RPr.RStyle.Val.
    3. `TestSetStyleClearParagraph`: SetStyle("") clears PStyle (nil).
    4. `TestSetStyleClearRun`: SetStyle("") clears RStyle (nil).
    5. `TestSetStyleChain`: `p.SetStyle("Heading1").SetAlignment(AlignmentCenter)` — both set correctly.
    6. `TestSetStyleOnExistingParagraph`: Open fixture doc (Phase 3) → Paragraphs()[0].SetStyle("Heading1") → sets PStyle → dirty → Save → content round-trips cleanly.
    7. `TestStyleWarningOnSave`: SetStyle with null byte in name → warning in Warnings().
    8. `TestStyleNilReceiverPanic`: Calling SetStyle on nil *Paragraph → panic.

    **QUAL-01 tests (io.ReaderAt/io.Writer):**
    9. `TestQualIOReaderAt`: Verify `OpenReader` exists and accepts io.ReaderAt. Already tested in Phase 3. Smoke test: open fixture via OpenReader → Paragraphs → no panic.
    10. `TestQualIOWriter`: Save via WriteTo to bytes.Buffer (already tested in existing tests). Explicit test: create formatted doc → WriteTo buffer → OpenReader from buffer → read back paragraphs.
    11. `TestQualIORoundTrip`:

    **QUAL-03 tests (X() escape hatch):**
    12. `TestQualRunX`: Verify Run.X() returns *wml.CT_R. Create run → X() → modify CT_R directly → verify dirty propagated.
    13. `TestQualParagraphX`: Paragraph.X() returns *wml.CT_P (existing). Verify it still works.
    14. `TestQualDocumentX`: Document.X() returns *opc.Package (existing).

    **Integration tests:**
    15. `TestFullContentDoc`: Build a document with:
       - Multiple paragraphs with different styles
       - Runs with varied formatting (bold, italic, underline, font, size, color, highlight)
       - Paragraph formatting (alignment, spacing, indent)
       - Named styles on both paragraphs and runs
       Save → Open → verify content via Paragraphs() accessors matches what was constructed.
       Verify Warnings() is clean for valid content.
    16. `TestStylePartsUntouched`: Open fixture doc → SetStyle on existing paragraph → Save to buffer → unzip → verify styles.xml is byte-identical to original (per-part diff, same as roundtrip_test.go).
    17. `TestFromTemplateWithStyle`: FromTemplate → AddParagraph → SetStyle → Save → verify style reference in output. Style parts should be the template's cloned styles.
  </action>
  <verify>
    <automated>go test ./... -v -count=1 2>&1 | tail -40</automated>
  </verify>
  <done>
    - All API-03 style application tests pass
    - QUAL-01: OpenReader/WriteTo work with content-rich documents
    - QUAL-03: Run.X() returns *wml.CT_R, all wrappers have X()
    - Full content doc test: build, save, re-open, verify
    - Style parts byte-identity test: untouched after SetStyle
    - FromTemplate + SetStyle integration works
    - All existing Phase 1-4 tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→resolver | Public SetStyle sets style name string on internal WML; resolver stays internal |
| in-memory→serialized | Body CT_Document serialized with style references — style parts never re-encoded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | Style name XML injection via SetStyle | low | mitigate | SetStyle stores name in typed CT_PStyle.Val *string — no raw XML fragment. xmlutil.NewEncoder escapes string values (Go encoding/xml default behavior). |
| T-04-06 | Tampering | Unintended style part modification | high | mitigate | D-06 enforced: serializeBody only calls MarkModified("word/document.xml"). Never touches styles/theme/numbering/fontTable/settings. Style parts pass-through unchanged in OPC Save. |
| T-04-07 | Information Disclosure | Warnings() with style reference details | low | accept | Warnings include style name strings that user provided. Same risk as any validation message. Style names are user-controlled inputs. |
| T-04-SC | Tampering | Package supply chain | high | mitigate | Zero external deps — Go stdlib only. No new packages in 04-02. |
</threat_model>

<verification>
1. All 04-01 tests + 04-02 tests pass: `go test ./... -count=1`
2. Style parts byte-identity test passes (styles.xml unchanged after SetStyle on body content)
3. Full content doc integration test: build → save → re-open → verify
4. Run.X() returns *wml.CT_R; all existing X() escape hatches unchanged
5. FromTemplate → SetStyle produces valid output with correct style references
</verification>

<success_criteria>
1. Paragraph.SetStyle sets pStyle reference; Run.SetStyle sets rStyle reference
2. Clean style names set without warning; empty string clears style reference
3. Style parts never MarkModified — byte-identity verified per-part
4. Resolver stays internal — no public accessor
5. QUAL-01: OpenReader/WriteTo I/O paths work with content (existing + verified)
6. QUAL-03: Run.X() escape hatch; all wrappers expose X()
7. Full content doc integration test passes
8. All existing tests pass
</success_criteria>

<output>
Create `.planning/phases/04-content-api/04-02-SUMMARY.md` when done.
</output>
