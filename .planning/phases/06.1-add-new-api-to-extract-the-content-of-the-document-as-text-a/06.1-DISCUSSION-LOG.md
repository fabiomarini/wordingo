# Phase 06.1: Text Extraction & Markdown Conversion — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-26
**Phase:** 06.1-text-extraction-markdown
**Areas discussed:** Text extraction API shape, Markdown export scope, Markdown import scope, Code organization & dependencies

---

## Text Extraction API Shape

| Option | Description | Selected |
|--------|-------------|----------|
| Body only | Paragraphs + tables in body. Simpler, predictable. | |
| Everything | Body + tables + headers + footers. More complete. | |
| Options struct | doc.ExtractText(opts) with flags. Follows MergeOpts pattern. | ✓ |

**User's choice:** Options struct
**Notes:** Follow Phase 6 MergeOpts pattern for consistency.

| Option | Description | Selected |
|--------|-------------|----------|
| Newlines only | One paragraph per line. Table cells tab-separated. | |
| Blank line between blocks | Double newline between blocks. | |
| Configurable separator | ExtractOpts.Separator string field. | ✓ |

**User's choice:** Configurable separator

| Option | Description | Selected |
|--------|-------------|----------|
| Skip silently | Only w:t text content. Clean output. | ✓ |
| Placeholder markers | [image], [page break], [hyperlink: url] | |
| Hyperlink: text + URL | Skip images/breaks, include hyperlink text + URL. | |

**User's choice:** Skip silently

| Option | Description | Selected |
|--------|-------------|----------|
| string | doc.ExtractText(opts) (string, error) | ✓ |
| []ExtractBlock | Structured slice with metadata. | |

**User's choice:** string (simple return type)

---

## Markdown Export Scope (docx → GFM)

| Option | Description | Selected |
|--------|-------------|----------|
| Headings + paragraphs + lists + code | Covers 90%+ | |
| Everything + tables | Full GFM including pipe tables | ✓ |
| Minimal | Headings and paragraphs only | |

**User's choice:** Everything + tables

| Option | Description | Selected |
|--------|-------------|----------|
| Standard GFM subset | **, *, ~~, ` — strip rest | ✓ |
| Bold + italic only | Simplest | |
| Bold/italic + HTML fallback | <u>, <span> for non-GFM | |

**User's choice:** Standard GFM subset

| Option | Description | Selected |
|--------|-------------|----------|
| Hyperlinks: text [url]; rest skip | | |
| Skip everything non-GFM | | |
| Hyperlinks only | | |

**User's choice:** (freeform) Hyperlinks → [text](url). Images → embedded base64 (existing PNG/JPEG only). Headers/footers → per ExtractOpts. Page breaks → skip.

| Option | Description | Selected |
|--------|-------------|----------|
| Existing images only | Only drawing.go PNG/JPEG | ✓ |
| All image relationships | Broader coverage | |

**User's choice:** Existing images only

---

## Markdown Import Scope (GFM → docx)

| Option | Description | Selected |
|--------|-------------|----------|
| Headings, paragraphs, lists, tables, code | Full useful subset | ✓ |
| Headings + paragraphs only | Simplest | |
| Everything incl. blockquotes | Most complete | |

**User's choice:** Headings, paragraphs, lists, tables, code blocks

| Option | Description | Selected |
|--------|-------------|----------|
| Bold, italic, code, links, images | Standard inline | ✓ |
| Bold + italic only | Simpler | |
| Everything incl. strikethrough | Full coverage | |

**User's choice:** Bold, italic, code, links, images

| Option | Description | Selected |
|--------|-------------|----------|
| Default heading styles | Heading1-6 | ✓ |
| Custom style prefix | Custom named styles | |
| Direct formatting | No named styles | |

**User's choice:** Default heading styles (Heading1-6)

| Option | Description | Selected |
|--------|-------------|----------|
| CreateFromMarkdown | Standalone function | |
| doc.ImportMarkdown | Method on Document | |
| Both | Both APIs | ✓ |

**User's choice:** Both

---

## Code Organization & Dependencies

| Option | Description | Selected |
|--------|-------------|----------|
| extract.go + markdown.go + markdown_import.go | Three root-level files | ✓ |
| Single convert.go | One file | |
| New markdown/ subpackage | Subpackage, leaner root | |

**User's choice:** Three files (extract.go, markdown.go, markdown_import.go)

| Option | Description | Selected |
|--------|-------------|----------|
| Stdlib-only parser | Custom GFM parser, no deps | ✓ |
| Use goldmark | Popular MIT parser, adds dep | |

**User's choice:** Stdlib-only parser (zero deps)

| Option | Description | Selected |
|--------|-------------|----------|
| doc.ToMarkdown(opts) string | Method on Document | ✓ |
| Standalone ConvertToMarkdown(doc) | Standalone function | |

**User's choice:** doc.ToMarkdown(opts) string

---

## the agent's Discretion

- ExtractOpts struct field naming and zero-value defaults
- Separator behavior at end of last element (trailing separator or not)
- Pipe table column alignment parsing (alignment from `:---` vs `----`)
- Code block language hint handling (stored as paragraph style or skipped)
- List nesting depth limit (9 levels per OOXML, practical limit lower)
- Base64 image format detection (PNG vs JPEG from bytes)
- Table cell content parsing (inline formatting inside table cells)
- Error handling for malformed markdown (best-effort parse vs strict)

## Deferred Ideas

None.
