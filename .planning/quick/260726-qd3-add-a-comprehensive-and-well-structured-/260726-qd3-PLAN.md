---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - examples/01-blank-doc/README.md
  - examples/02-text-and-styles/README.md
  - examples/03-tables/README.md
  - examples/04-images/README.md
  - examples/05-headers-footers/README.md
  - examples/06-lists/README.md
  - examples/07-hyperlinks/README.md
  - examples/08-comprehensive/README.md
  - examples/09-merge-and-edit/README.md
  - examples/10-template-to-document/README.md
  - examples/11-text-extraction-markdown/README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "README.md exists at repo root and documents every public Go API surface in wordingo"
    - "Every examples/*/ directory has a README.md describing what it demonstrates, how to run it, what output it produces, and a code walkthrough"
    - "All technical claims in READMEs are derived from actual source — no invented features or functions"
    - "The zero-dependency claim is verifiable against go.mod (stdlib + internal packages only)"
    - "Default style IDs are documented accurately as Italian-named (Normale, Titolo, Titolo1–Titolo9, Sottotitolo); English style names in examples are noted as producing Warnings()"
  artifacts:
    - "README.md"
    - "examples/01-blank-doc/README.md"
    - "examples/02-text-and-styles/README.md"
    - "examples/03-tables/README.md"
    - "examples/04-images/README.md"
    - "examples/05-headers-footers/README.md"
    - "examples/06-lists/README.md"
    - "examples/07-hyperlinks/README.md"
    - "examples/08-comprehensive/README.md"
    - "examples/09-merge-and-edit/README.md"
    - "examples/10-template-to-document/README.md"
    - "examples/11-text-extraction-markdown/README.md"
  key_links:
    - "Root README examples index links to each examples/*/README.md (relative links that resolve from repo root)"
---

<objective>
Add a comprehensive, accurate root README.md covering the full wordingo public API, plus a README.md inside each of the 11 example directories explaining what the example demonstrates, how to run it, what it outputs, and a walkthrough of the non-trivial code.

Purpose: Make the project discoverable and self-documenting for Go developers evaluating the library, and make each example runnable+understandable in isolation.

Output: 1 root README.md + 11 example README.md files (example 11 already has one — replace it with a consistent-structured version aligned with the other 10).
</objective>

<execution_context>
@/Users/fabiomarini/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/fabiomarini/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@./go.mod
@./LICENSE
@./wordingo.go
@./paragraph.go
@./run.go
@./format.go
@./table.go
@./image.go
@./hyperlink.go
@./header.go
@./list.go
@./page.go
@./body.go
@./markdown.go
@./markdown_import.go
@./merge.go
@./extract.go
@./template.go
@./open.go
@./create.go
@./defaults/styles.xml
@./examples/01-blank-doc/main.go
@./examples/02-text-and-styles/main.go
@./examples/03-tables/main.go
@./examples/04-images/main.go
@./examples/05-headers-footers/main.go
@./examples/06-lists/main.go
@./examples/07-hyperlinks/main.go
@./examples/08-comprehensive/main.go
@./examples/09-merge-and-edit/main.go
@./examples/10-template-to-document/main.go
@./examples/11-text-extraction-markdown/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the root README.md</name>
  <files>README.md</files>
  <behavior>
    - Public API surface captured: Create, Open, OpenReader, FromTemplate, FromTemplateReader, OpenTemplate, OpenTemplateReader, CreateFromMarkdown; Document methods .
    - Paragraph methods: Style/Text/X/AddRun/AddHyperlink/SetAlignment/SetSpacing/SetIndent/SetStyle/SetPageBreakBefore/SetFormatting.
    - Run methods: X/SetBold/SetItalic/SetUnderline/SetFont/SetSize/SetColor/SetHighlight/SetStyle/SetText/ReplaceText/SetFormatting/SetImageWidth/SetImageHeight.
    - TableBuilder/RowBuilder/CellBuilder methods listed.
    - ListBuilder methods: AddItem/X.
    - Header/Footer methods: Paragraphs/InsertParagraphAt/DeleteParagraphAt/AddParagraph/X.
    - Section methods: SetOrientation/SetPaperSize/SetMargins/X.
    - Body() / BodyElement / BodyElementType (ElementParagraph, ElementTable) documented.
    - ScopedParts + MergeOpts + ExtractOpts (ScopedParts{Body,Tables,Headers,Footers}, Separator) documented.
    - Alignment constants, HeaderVariant/FooterVariant constants, PageOrientation constants, paper size constants listed.
    - Default styles documented with ACTUAL IDs from defaults/styles.xml (Italian): Normale, Titolo, Titolo1..Titolo9, Sottotitolo, Citazione, Enfasiintensa, Citazioneintensa, Riferimentointenso, Paragrafoelenco. Note that examples in this repo reference English style IDs (Title, Heading1, Heading2, Heading3) for clarity; those produce non-fatal warnings via doc.Warnings() against the default blank-document style table but the file still opens in Word. To use English-named styles, supply a template via FromTemplate/OpenTemplate whose styles.xml defines them.
    - Zero-dependency claim verifiable: go.mod has no require directives; only stdlib (archive/zip, bytes, encoding/base64, encoding/xml, fmt, image/jpeg, image/png, io, math, os, path, regexp, strings) + internal/{opc,wml,xmlutil,style,markdown} subpackages.
  </behavior>
  <action>
Create the root README.md with sections in this exact top-down order (most important first):

1. **Title + tagline**: `# wordingo` followed by a one-line tagline: "Pure Go, zero-dependency library for creating and editing Microsoft Word (.docx) documents — built on the OOXML (WordprocessingML) standard."

2. **What it is** (2-3 short paragraphs): pure Go, MIT-licensed, stdlib-only; creates styled .docx from scratch OR opens existing docs for round-trip editing OR clones a template's style dependency graph; opening in Word without repair is the validation bar. State it is a library (not a CLI). Mention scope: ~60 essential WML types modeled, unknown elements preserved not parsed.

3. **Key features** (bulleted list, one line each):
   - Create blank documents with default styles, theme, font table, settings (Letter, 1-inch margins)
   - Open existing .docx and round-trip them with zero unintended changes
   - Clone a template's entire style dependency graph (styles.xml, numbering.xml, fontTable.xml, theme.xml, settings.xml) into a fresh body via FromTemplate, or preserve body content via OpenTemplate
   - Paragraphs, runs, and named styles (paragraph- and run-level)
   - Rich inline formatting: bold, italic, underline, font, size (half-points), color (hex), highlight, style
   - Tables: simple grid (AddTable from [][]string) and full fluent builder with style, width, borders, shading, per-cell shading/bold/width, MergeRight/MergeDown, DeleteRow
   - Images: PNG + JPEG embedded as DrawingML inline; DPI detection from JFIF/EXIF/PNG pHYs; auto-size or explicit SetImageWidth/SetImageHeight in inches; locked aspect ratio, stretch-to-fill
   - Headers and footers: default/first/even variants; OPC parts created lazily; header/footer paragraphs editable via InsertParagraphAt/DeleteParagraphAt/AddParagraph
   - Ordered and bulleted lists with full 9-level nesting depth (levels 0-8); AddList, AddListFromSlice, AddNumberingDef (custom numFmt/start); numbering.xml merged with any template numbering
   - Hyperlinks with fresh OPC relationships and Run-style chaining (SetBold/SetColor/SetUnderline on the returned *Run)
   - Page setup: orientation (portrait/landscape), paper size (Letter/A4/Legal constants; custom twips), margins (twips), page breaks (AddPageBreak or SetPageBreakBefore); chainable on Document
   - Template merge: {{placeholder}} replacement in body, table cells, headers, footers; handles split-run placeholders; scoped via MergeOpts.ScopedParts; warns on unused keys
   - Edit operations: InsertBefore/InsertAfter by paragraph pointer identity, DeleteParagraph by identity, Run.SetText, Run.ReplaceText, TableBuilder.DeleteRow
   - Body() ordered iteration mixing paragraphs and tables (BodyElement.type = ElementParagraph | ElementTable)
   - Text extraction: doc.ExtractText(ExtractOpts) — plain text from body/tables/headers/footers, scoped, custom separator
   - Markdown export: doc.ToMarkdown(ExtractOpts) — GFM output (headings, bold/italic, pipe tables, links, image data URIs, lists, code blocks)
   - Markdown import: CreateFromMarkdown(string) builds a new Document; doc.ImportMarkdown(string) appends to an existing doc; supports headings, paragraphs, ordered/bulleted lists, pipe tables, fenced code blocks, inline bold/italic/code/links/data-URI images
   - Validation surface: doc.Warnings() returns non-fatal issues (unknown style refs, invalid colors, negative sizes, unused merge keys, markdown parse errors, package-layer warnings)
   - Escape hatch via X() methods on Document, Paragraph, Run, TableBuilder, RowBuilder, CellBuilder, ListBuilder, Header, Footer, Section for raw WML access

4. **Installation** — `go get github.com/fabiomarini/wordingo`. Requires Go 1.23 (from go.mod). Note: module path is github.com/fabiomarini/wordingo.

5. **Quick start** — a compact code block (10-12 lines) showing Create → AddParagraph with style → AddRun formatting → AddTable → Save, producing output.docx. Match the actual API: `wordingo.Create()`, `doc.AddParagraph(text).SetStyle("...")`, `p.AddRun("...").SetBold(true)`, `doc.AddTable([][]string{...})`, `doc.Save("output.docx")`, `defer doc.Close()`.

6. **Comprehensive feature walkthrough** — subsections with one small code snippet each, in this order, each snippet ≤15 lines and using actual function signatures:
   - Creating & opening documents (Create, Open, OpenReader, Save/WriteTo/SaveFile, Close)
   - Templates (FromTemplate, OpenTemplate — explain difference: FromTemplate = fresh body with template styles; OpenTemplate = preserved body content; both clone the style dependency graph)
   - Paragraphs & runs (AddParagraph, AddRun, SetText, ReplaceText)
   - Paragraph formatting (SetAlignment with AlignmentLeft/Center/Right/Both, SetSpacing ParSpacing{Before,After,Line,LineRule}, SetIndent ParIndent{Left,Right,FirstLine,Hanging}, SetPageBreakBefore, SetFormatting with ParFormat)
   - Run formatting (SetBold, SetItalic, SetUnderline, SetFont, SetSize points, SetColor 6-hex, SetHighlight, SetStyle, SetFormatting with RunFormat)
   - Named styles — STATE THE TRUTH: blank documents created via Create() ship with Italian-named styles from defaults/styles.xml: Normale, Titolo, Titolo1..Titolo9, Sottotitolo, Citazione, Citazioneintensa, Enfasiintensa, Riferimentointenso, Paragrafoelenco. Paragraph.SetStyle(name) and Run.SetStyle(name) set the style ID; unknown IDs produce a non-fatal warning via doc.Warnings(). To use English-named styles (Title, Heading1…), provide a template doc whose styles.xml defines them. Show Paragraph.SetStyle("Titolo") and Run.SetStyle("...").
   - Tables (AddTable [][]string → TableBuilder; AddTableBuilder fluent; Row(idx).Cell(idx).SetText/SetBold/SetWidth/SetShading/MergeRight/MergeDown; SetTableStyle/SetWidth/SetBorders/SetShading; DeleteRow(idx); Tables() accessor)
   - Images (AddImage(path) and AddImageBytes(name,data,ct); returns *Run; run.SetImageWidth(inches)/SetImageHeight(inches); PNG+JPEG DPI auto-detect; DrawingML inline)
   - Headers & footers (AddHeader(HeaderDefault|HeaderFirst|HeaderEven), AddFooter(FooterDefault|FooterFirst|FooterEven); returned Header/Footer expose Paragraphs/InsertParagraphAt/DeleteParagraphAt/AddParagraph)
   - Lists (AddList(ordered bool) → ListBuilder.AddItem(text, level 0-8); AddListFromSlice([]string, ordered); AddNumberingDef(numFmt, start) for custom formats like "upperRoman"; 9-level nesting)
   - Hyperlinks (para.AddHyperlink(text, uri) → *Run; chain SetBold/SetColor/SetUnderline; one relationship per call)
   - Page setup (doc.SetOrientation(OrientationPortrait|Landscape), doc.SetPaperSize(PaperLetterW,H | PaperA4W,H | PaperLegalW,H or custom twips), doc.SetMargins(top,right,bottom,left twips); doc.AddPageBreak(); or via doc.Section().Set*)
   - Template merge (doc.Merge(map[string]string, *MergeOpts); handles {{key}} including across run boundaries; MergeOpts{ScopedParts{Body,Tables,Headers,Footers}}; unused keys → warning)
   - Edit operations (doc.InsertBefore(target *Paragraph, text), doc.InsertAfter(target, text), doc.DeleteParagraph(target); run.SetText, run.ReplaceText; tbl.DeleteRow(idx); doc.Paragraphs(), doc.Tables(), doc.Body())
   - Text extraction (doc.ExtractText(&ExtractOpts{ScopedParts, Separator}); nil opts = all parts + "\n")
   - Markdown (doc.ToMarkdown(&ExtractOpts), wordingo.CreateFromMarkdown(string), doc.ImportMarkdown(string))
   - Validation & escape hatch (doc.Warnings(); X() methods on Document/Paragraph/Run/TableBuilder/RowBuilder/CellBuilder/ListBuilder/Header/Footer/Section return underlying WML types)

7. **Examples** — a numbered list linking to each example directory's README with a one-line description of what it demonstrates. Use relative links: `examples/01-blank-doc/` (link to `examples/01-blank-doc/README.md`). The 11 entries:
   1. blank-doc — minimum Hello World
   2. text-and-styles — paragraphs, headings, inline run formatting
   3. tables — simple grid + fluent builder with borders, shading, cell width
   4. images — embedded PNG with explicit size
   5. headers-footers — header/footer, A4, landscape, margins, page break
   6. lists — bulleted, ordered, nested outline, AddListFromSlice
   7. hyperlinks — inline links with chained Run formatting
   8. comprehensive — every feature in one doc
   9. merge-and-edit — {{placeholder}} Merge, InsertBefore/After, DeleteParagraph, DeleteRow, ReplaceText/SetText, scoped Merge, Body() iteration, Warnings()
   10. template-to-document — FromTemplate (fresh body), OpenTemplate (preserved body), FromTemplate+Merge+InsertBefore combined
   11. text-extraction-markdown — ExtractText, ToMarkdown, CreateFromMarkdown, ImportMarkdown, scoped parts, round-trip verification

8. **Design & philosophy** (short section): pure Go stdlib only (zero external dependencies; go.mod has no require directives); ISO/IEC 29500 OOXML is the authoritative spec; ~60 essential WML types modeled, unknown XML elements preserved verbatim not lossy-parsed; style fidelity is the product (clone the style dependency graph, never copy files); open-in-Word-without-repair is the validation bar; escape hatch via X() rather than bloating the public surface.

9. **Status** — v1.0 milestone complete (Phases 1-6 + 06.1 all shipped). Out of scope for v1 (and not implemented): PDF conversion, .doc/.odt/.rtf/.xlsx/.pptx, CLI tool, watch mode, HTML rendering, Word COM automation/CGO, field codes, comments, bookmarks, content controls, tracked changes, charts, equations. These are deferred to v2.

10. **License** — MIT, copyright 2026 Fabio Marini. See LICENSE.

11. **Contributing** — short note: PRs welcome; open an issue first for non-trivial changes; keep the zero-dependency invariant (stdlib + internal packages only); run `go test ./...` before submitting.

Keep prose tight. Code snippets must compile against the real API (use `wordingo.` package qualifier in snippets to match examples). Do NOT invent fields, methods, options, or constants that are not present in the source files listed in <context>. When unsure about a signature, omit the snippet rather than guess — cross-reference the actual .go files.

Do NOT add any markdown badges, logo images, or sponsor sections. Do NOT add a Table of Contents (the file is short enough).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('README.md','utf8');const must=['# wordingo','Quick start','Installation','Examples','Design','License','Contributing','CreateFromMarkdown','FromTemplate','OpenTemplate','AddTable','AddList','AddImage','AddHeader','AddFooter','Merge','ExtractText','ToMarkdown','ImportMarkdown','AddHyperlink','SetImageWidth','BodyElement','HeaderDefault','FooterDefault','AlignmentLeft','PaperLetterW','Warnings','Normale','Titolo1','zero-dependency','github.com/fabiomarini/wordingo','MIT','Go 1.23'];const missing=must.filter(m=>!s.includes(m));if(missing.length){console.error('MISSING:',missing);process.exit(1)}if(!s.includes('examples/01-blank-doc/README.md')||!s.includes('examples/11-text-extraction-markdown/README.md')){console.error('missing example links');process.exit(1)}console.log('OK',s.length,'bytes')"</automated>
  </verify>
  <done>
    README.md exists at repo root, contains all required sections in the specified order, references every public API in the wordingo package, documents the actual Italian-named default styles, links to all 11 example READMEs, and passes the missing-token grep above against actual function/style names pulled from source.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write a README.md inside each example directory</name>
  <files>examples/01-blank-doc/README.md, examples/02-text-and-styles/README.md, examples/03-tables/README.md, examples/04-images/README.md, examples/05-headers-footers/README.md, examples/06-lists/README.md, examples/07-hyperlinks/README.md, examples/08-comprehensive/README.md, examples/09-merge-and-edit/README.md, examples/10-template-to-document/README.md, examples/11-text-extraction-markdown/README.md</files>
  <behavior>
    - Each example README uses the same 4-section template: "What it demonstrates", "How to run", "Output", "Code walkthrough".
    - "How to run" command is `go run main.go` (verified: every example dir has a single main.go entry file).
    - "Output" lists the actual output file(s) the example writes. Verified per example: 01→output.docx; 02→output.docx; 03→output.docx; 04→output.docx; 05→output.docx; 06→output.docx; 07→output.docx; 08→output.docx; 09→output.docx; 10→from-template-report.docx, open-template-appendix.docx, template-merge-invoice.docx; 11→output.docx, output.txt, output.md, imported-source.md, imported-output.txt, imported-output.md.
    - "Code walkthrough" identifies 3-6 non-trivial lines from that example's main.go and explains what each does in terms of the public API it calls. Direct quotes of code lines are OK; explanations must reference the real API signatures used.
    - Example 10 notes the testdata dependency: it reads ../testdata/word/03-custom_styles.docx and 04-custom_styles_plus_sample_text.docx via runtime.Caller path resolution — so it must be run from its own directory (`cd examples/10-template-to-document && go run main.go`) or it will fail to locate testdata.
    - Example 11 README replaces the existing one with the same 4-section template but preserves the technical content (GFM export, scoped parts, round-trip verification) in the walkthrough section.
    - No README claims a feature, function, or behavior not present in that example's main.go.
  </behavior>
  <action>
For each of the 11 example directories, write a README.md following the identical structure:

```
# Example NN: <Title>

## What it demonstrates
<2-4 sentences describing the feature surface this example exercises, grounded in the actual main.go imports and calls>

## How to run
<Shell block>:
cd examples/NN-<slug>
go run main.go
<For example 10 ONLY, add a note: "Requires the testdata/word/*.docx files at the repo root; uses runtime.Caller to locate them, so run from this directory.">

## Output
<Bulleted list of files the example writes to disk, exactly matching the Save/WriteFile calls in main.go>
- output.docx — <one-line description>
<For example 10: three .docx files. For example 11: output.docx, output.txt, output.md, imported-source.md, imported-output.txt, imported-output.md>

## Code walkthrough
<A numbered or bulleted list of 3-6 key code lines excerpted from main.go (the non-trivial ones), each followed by a 1-2 sentence explanation of the API call it makes and why it matters. Reference real identifiers from the file: e.g. for 01 — wordingo.Create(), doc.AddParagraph("Hello, World!"), doc.Save(out), defer doc.Close(); for 07 — para.AddHyperlink("Go Programming Language", "https://go.dev").SetColor("0563C1").SetUnderline("single"); for 09 — doc.Merge(map[string]string{...}, nil), doc.InsertBefore(introPara, "Dear Alice,"), tbl.DeleteRow(2), doc.Body() iteration with ElementParagraph/ElementTable; for 11 — doc.ExtractText(&wordingo.ExtractOpts{ScopedParts:...}), doc.ToMarkdown(nil), wordingo.CreateFromMarkdown(sourceMD), imported.ImportMarkdown("...").>
```

Specific per-example guidance:

01-blank-doc — Walkthrough: wordingo.Create() builds a blank OPC package with default styles; defer doc.Close() releases packages; doc.AddParagraph("Hello, World!") appends a paragraph with one run; doc.Save(out) serializes the package to a ZIP .docx. Note fmt.Printf reports byte size via os.Stat.

02-text-and-styles — Walkthrough: SetStyle("Title") on first paragraph (note in README that default blank-doc style table uses Italian IDs — Titolo, Sottotitolo, Titolo1..Titolo3 — so "Title"/"Heading2" etc. emit Warnings(); the file still opens); p.AddRun(...).SetBold(true) appends a styled run to an existing paragraph; r.AddRun("...").SetBold(true).SetColor("2E75B6") chains run formatting.

03-tables — Walkthrough: doc.AddTable([][]string{...}) — simple grid from string matrix, returns TableBuilder; doc.AddTableBuilder() — empty fluent table; tbl.SetTableStyle("LightGridAccent1") and tbl.SetBorders(&wordingo.TableBorders{...}) configure table-level props; tbl.Row(0).Cell(0).SetText("Product").SetBold(true).SetWidth(1500,"dxa") shows chained per-cell builders with twips width.

04-images — Walkthrough: makeGradientPNG() helper produces a 100x100 PNG via image/png.Encode from stdlib; doc.AddImageBytes("gradient.png", imgData, "image/png") returns (*Run, error); run.SetImageWidth(3.0).SetImageHeight(2.0) sets display size in inches; auto-DPI detection handles the rest.

05-headers-footers — Walkthrough: doc.AddHeader(wordingo.HeaderDefault).AddParagraph("Report — Confidential") creates a header part and adds a paragraph in one chain; doc.SetPaperSize(PaperA4W, PaperA4H), doc.SetMargins(1440,1440,1440,1440), doc.SetOrientation(OrientationLandscape) configure the single section; doc.AddParagraph("").SetPageBreakBefore(true) inserts a page-break paragraph.

06-lists — Walkthrough: doc.AddList(false) for bulleted, doc.AddList(true) for ordered — both return *ListBuilder; list.AddItem("Apples", 0) adds item at level 0; nested outline uses levels 0/1/2 to show 9-level depth support; doc.AddListFromSlice([]string{"One","Two","Three","Four"}, true) convenience constructor.

07-hyperlinks — Walkthrough: para.AddHyperlink("Go Programming Language", "https://go.dev").SetColor("0563C1").SetUnderline("single") — AddHyperlink returns a *Run supporting the same formatting chainers as runs; each call allocates a fresh OPC relationship in word/_rels/document.xml.rels with TargetMode="External".

08-comprehensive — Walkthrough: one example exercising headers/footers, paper size, Title/Subtitle/Heading1 styles (with the same Italian-ID caveat), inline run formatting (bold/italic/underline/color/font/size), simple grid + fluent builder tables with shading, embedded PNG image, bulleted + ordered lists, hyperlink, page break, and landscape section via doc.SetOrientation(OrientationLandscape) + doc.SetMargins on the same single section.

09-merge-and-edit — Walkthrough: doc.AddHeader(HeaderDefault).AddParagraph("Report: {{report_name}} — {{date}}") defines a header with placeholders; doc.Merge(map[string]string{...}, nil) replaces {{key}} across body, table cells, headers, footers (split-run safe); doc.InsertBefore(introPara, "Dear Alice,") / doc.InsertAfter / doc.DeleteParagraph(target) are pointer-identity based; run.SetText and run.ReplaceText mutate existing runs; doc.Body() iterates in document order returning BodyElement{Type, Para, Table}; tables[0].DeleteRow(2) removes a row with bounds safety; a second scoped Merge(..., &MergeOpts{ScopedParts:{Headers:true,Footers:true}}) updates only the header/footer; doc.Warnings() surfaces unused keys and other non-fatal issues.

10-template-to-document — Walkthrough: wordingo.FromTemplate(path) clones the template's style dependency graph (styles, numbering, fontTable, theme, settings) into a fresh empty body — then doc.AddParagraph("...").SetStyle("Titolo") uses the ITALIAN style IDs from that template; wordingo.OpenTemplate(path) preserves the template's existing paragraphs/tables (header/footer refs in sectPr are stripped to avoid dangling rIds) so you can append with doc.AddParagraph / doc.AddTableBuilder; example 3 combines FromTemplate + Merge + InsertBefore to build an invoice. Note the testdataPath/runtime.Caller trick and printStyleIDs helper that greps styles.xml for w:styleId values.

11-text-extraction-markdown — Walkthrough (replace existing README, preserve technical content in the walkthrough): doc.ExtractText(nil) pulls plain text from body+tables+headers+footers with "\n" separator; doc.ExtractText(&wordingo.ExtractOpts{ScopedParts:..., Separator:" | "}) scopes which parts and custom separator; doc.ToMarkdown(nil) emits GFM with **bold**, *italic*, pipe tables, [text](url) links, ![alt](data:image/png;base64,...) image data URIs, list markers, code blocks; wordingo.CreateFromMarkdown(srcMD) builds a new Document from markdown (headings → Heading1..6 paragraph styles, lists, tables, code blocks, inline bold/italic/code/links/data-URI images); doc.ImportMarkdown(md) appends markdown to an existing doc (parse errors surface via Warnings, never panic); the example then runs ~18 verifyContains/verify* checks for round-trip fidelity. Note the makeGradientPNG helper uses a 50x50 image here (smaller than example 04's 100x100).

Accurate-claims checklist for EACH example README before writing — re-read that example's main.go (already in <context>) and confirm:
- The set of output files listed matches the Save/WriteFile/os.WriteFile calls in main.go (do not add or omit).
- The walkthrough line excerpts are real lines from main.go (verbatim or near-verbatim), not invented.
- Every API identifier named in the walkthrough exists in the public package (functions/methods/types/constants documented in Task 1 or visible in the source).
- No example README claims dependency on a file not actually needed by that example (e.g., only example 10 needs testdata).
- Standard "How to run" block is the literal `cd examples/NN-<slug>` + `go run main.go`. Do not suggest `go run .` unless that example's main.go is confirmed to be runnable both ways (it is, since each example dir is its own main package — but use `go run main.go` consistently for clarity).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),path=require('path');const ex=['01-blank-doc','02-text-and-styles','03-tables','04-images','05-headers-footers','06-lists','07-hyperlinks','08-comprehensive','09-merge-and-edit','10-template-to-document','11-text-extraction-markdown'];const errs=[];for(const e of ex){const p='examples/'+e+'/README.md';if(!fs.existsSync(p)){errs.push('missing '+p);continue}const s=fs.readFileSync(p,'utf8');for(const h of ['## What it demonstrates','## How to run','## Output','## Code walkthrough'])if(!s.includes(h))errs.push(e+' missing '+h);if(!s.includes('go run main.go'))errs.push(e+' missing run cmd');if(!s.includes('examples/'+e))errs.push(e+' missing self-ref in run cmd')}if(errs.length){console.error('FAIL:');errs.forEach(e=>console.error(' - '+e));process.exit(1)}console.log('OK 11 example READMEs, all sections present')"</automated>
  </verify>
  <done>
    All 11 examples/*/README.md files exist, each with the 4-section template (What it demonstrates / How to run / Output / Code walkthrough), each listing the correct output artifact(s) for that example, each walkthrough excerpting real lines from that example's main.go and referencing only public API identifiers that exist in the wordingo package. Example 10 README documents the testdata/runtime.Caller requirement. Example 11 README replaces the existing file with the consistent 4-section structure while preserving the GFM/scoped-parts/round-trip technical content.
  </done>
</task>

</tasks>

<verification>
- Both automated checks above print OK and exit 0.
- `go vet ./...` and `go build ./...` still pass (no source files touched).
- Spot-check: open README.md in the repo root and confirm the Examples section links to all 11 example directories; clicking any link (relative URL) resolves to the corresponding examples/NN-slug/README.md.
- Spot-check accuracy: in examples/09-merge-and-edit/README.md confirm DeleteRow index and scoped Merge ScopedParts fields match main.go; in examples/10-template-to-document/README.md confirm the three output filenames match; in examples/11-text-extraction-markdown/README.md confirm the 3 output docx/md/txt + 3 imported-* artifacts match main.go.
</verification>

<success_criteria>
- Root README.md is the single entry point for the library's public API, accurately documenting create/open/template/paragraph/run/table/image/header-footer/list/hyperlink/page-setup/merge/edit/extract/markdown/validation/escape-hatch surfaces in the specified section order.
- Default styles documented truthfully as Italian-named (from defaults/styles.xml), with the examples' English-named style IDs flagged as producing non-fatal warnings.
- Zero-dependency claim tied to go.mod (no require directives; Go 1.23).
- Every example directory has a structured README consistent across all 11, with verified output-file lists and real-code walkthroughs.
- No invented features, methods, or files anywhere.
</success_criteria>

<output>
Create `.planning/quick/260726-qd3-add-a-comprehensive-and-well-structured-/260726-qd3-SUMMARY.md` when done.
</output>